# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. MILESTONE: K5.8 (NoisyNet QR-DQN) 200k PASSED its in-env eval FROM SCRATCH with zero demonstrations (mean 980.7 > bar 930.27, two-sided non-degenerate). First from-scratch RL policy to clear the baseline; charter mission AND the strict purity bar now met by pure RL, pending reproducibility. A seed-1 reproduction is RUNNING. K5.9 DQfD-lite is deprioritized (the fallback for pure-RL failure, no longer needed). K5.7 (QR-DQN epsilon-greedy) closed FAIL. PPO/PPO-finetune/vanilla-DQN all rejected. Single-voice governance.

**Last commit:** `5aa4f15` handoff: K5.7 200k FAIL; K5.8 NoisyNet 200k launched detached; K5.9 de-risked

**Current task:** K5.8 NoisyNet 200k (seed 0) is COMPLETE and PASSED its final in-env eval on held-out seeds 1000-1009: mean 980.7 (bar 930.27, delta +50.4), action fractions L/Stay/R 0.055/0.862/0.082 (two-sided, non-degenerate: frac_L 0.055 and frac_R 0.082 both >= 0.03, max 0.862 < 0.97), collision_rate 0.9 (anchored to runs\phase_k\k5_8_noisy_qrdqn\eval_inenv report json, read this session; per-seed highlights seed 1001 full 1800 timeout survival, 1002 reached 1623). This is the FIRST from-scratch RL policy (NoisyNet exploration, ZERO demonstrations) to beat the constant-action baseline; the from-scratch/purity result that did not previously exist is now MET by pure RL. Three honest caveats: (1) modest margin (+50, ~5.4%), far below BC (1737.3); (2) it still dies 9/10 episodes and clears the bar only on mean survival length; (3) SINGLE training seed (seed 0), reproducibility unconfirmed. A seed-1 reproduction (tools\run_k5_8_s1_200k.bat, --seed 1 --out runs\phase_k\k5_8_noisy_qrdqn_s1) is RUNNING detached (python PID 12016 alive at 19:5x, ~3-4hr), writing runs\phase_k\k5_8_noisy_qrdqn_s1_200k.sentinel on completion. K5.9 DQfD-lite is DEPRIORITIZED; its de-risk notes (docs\k5_9_dqfd_notes.md, verified asset) stay on disk if ever wanted to push the margin higher. Above-baseline policies now: BC 1737.3, PPO-finetune 1710.5, K5.8 NoisyNet-from-scratch 980.7.

**Next action:** Check runs\phase_k\k5_8_noisy_qrdqn_s1_200k.sentinel. If DONE, eval the seed-1 model: set SIGHT_GODOT_EXE inline, python tools\k5_8_noisy_eval_inenv.py --run runs\phase_k\k5_8_noisy_qrdqn_s1 --seeds 1000-1009 (PASS only if mean>=930.27 AND frac_L>=0.03 AND frac_R>=0.03 AND max(frac)<0.97). If it PASSes too, the from-scratch result is reproducible across 2 seeds: render a demo clip of the seed-0 K5.8 policy (reuse the BC/ppo_ft render path on runs\phase_k\k5_8_noisy_qrdqn, seed 1009) and treat the milestone as locked. If seed-1 FAILs, seed-0 was a variance pass: run seed 2 (tools\run_k5_8_s1_200k.bat copy with --seed 2 --out ..._s2) and report the spread honestly (mean +/- across seeds); do NOT claim a robust from-scratch result on one lucky seed. If the seed-1 200k is still running, lean-poll (sleeps <=45s); do NOT launch anything while it holds the CPU. Do NOT revisit epsilon-greedy QR-DQN or reward-scaling. Wrap git in cmd /c "...".

**Blockers:** None hard. ONE Jeff-facing DIRECTION note (see note 1): the hard mission is essentially met; the next strategic fork (reproduce/harden further vs polish the demo and writeup vs call it done) is direction-level and Jeff-owned. Surface it, do not unilaterally expand scope into a big new push. Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable.

**Notes:**

- MILESTONE + DIRECTION: from-scratch RL (NoisyNet QR-DQN, zero demos) PASSED at mean 980.7 vs 930.27 bar, two-sided. This is the result that did not exist. Caveats: modest +5.4% margin, 0.9 collision rate, single seed. Reproducibility (seed-1 running) must confirm before any locked portfolio claim. The mission hard-version is met; surface the harden/polish/call-it fork to Jeff rather than auto-launching a big new effort.
- WHY IT WORKED (vs K5.7): only the exploration MECHANISM changed (NoisyNet replacing epsilon-greedy; identical net_arch, env, reward "none", gamma 0.99). epsilon-greedy QR-DQN went two-sided-but-incompetent (547.7 FAIL); NoisyNet's consistent within-episode perturbation produced competent dodging (980.7 PASS). Confirms the wall was the exploration mechanism, not env/value/reward geometry. The "change the method, do not retry harder" rule paid off. Do not revisit epsilon-greedy or reward-scaling.
- K5.9 DQfD-lite DEPRIORITIZED: it was the fallback for pure-RL failure; pure RL succeeded. The de-risk notes (docs\k5_9_dqfd_notes.md, verified asset runs\phase_k\k5_6_bc\dataset_2000_2035.npz) remain on disk if ever wanted to push the margin well above the bar, but it is not needed for the charter or the purity bar.
- The detached waiter/sentinel pattern carried the full K5.7->K5.8 chain unattended last session and the K5.8 200k -> eval -> seed-1 repro launches this session. Reliable; standard tool now (tools\run_k5_8_eval.bat, tools\run_k5_8_s1_200k.bat added this session).
- SCOPE/honesty for any writeup: K5.8 is GENUINE from-scratch RL (no demonstrations, NoisyNet exploration); BC is imitation learning. Do not conflate the two. The from-scratch claim is currently single-seed (N=1 training seed, 10 eval seeds); state N and reproducibility status honestly. reward_shaping "none" stays load-bearing (best constant 845.7 < bar 930.27). runs/ is gitignored.
