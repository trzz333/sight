# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K (K3.5c complete: production reward-scaling train patch applied; scaled training at divisor 30 on seed 0 for 2048 ts and 10000 ts both completed cleanly under the patched trainer; trained-only H5 external eval at seeds 1000-1009 ran against both checkpoints. K3.5c classification per the GPT contract: **partial break**. K3.5c per-seed eval differs from canonical Phase D (10/10 lengths differ, the timeout slot shifted from seed 1001 to seed 1008) but the 2048 and 10000 K3.5c checkpoints produce bit-identical per-seed eval results despite distinct model SHA-256 hashes. The original Phase K eval anomaly is partially resolved: reward scaling moves the eval surface off the Phase B/C/D fixed point, but the deterministic-argmax wedge persists within a single K3.5 training regime. K4 is warranted, narrower than before).

**Last commit:** `334af43` Phase K K3.5c direct eval falsification: partial break, K4 narrowed (substantive). Production train patch in prior commit `02bb809`. Chore handoff refresh follows in this push.

**Current task:** K3.5c direct eval-anomaly falsification per the GPT Path 2 scope. Production trainer patched to apply `FixedRewardScaleVecEnv` to the train env only via `--reward-scale-divisor FLOAT`. Two scaled seed-0 training slices executed: K3.5c 2048 ts (79 s wall, 28 PPO updates) and K3.5c 10000 ts (409 s wall, 156 PPO updates), both with `reward_scale_divisor=30.0` and `reward_scale_applied=true` recorded in `summary.json` and the `run_start` event. Trained-only H5 external eval on seeds 1000-1009 ran against both checkpoints in `--mode full --policies trained_cnn` against the canonical `signal_dodge_ppo_h5_pixel_entropy.yaml`. Eval VecEnv is NOT reward-scaled. Both eval summaries report `passed=True saturated_negative_controls=[]`. Per-seed lengths and terminations are bit-identical between the 2048 and 10000 K3.5c evals (10/10 length match, 10/10 termination match) while differing from canonical Phase D (0/10 length match, timeout slot shifted from Phase D seed 1001 to K3.5c seed 1008). Model artifacts are distinct: K3.5c 2048 model SHA-256 `E14D1A12...` vs K3.5c 10000 model SHA-256 `5664A12E...`. K3.5c aggregate mean length 845.7 vs Phase D 689.7 (+22 percent); collision rate 0.9 and timeout rate 0.1 coincide between the configs by rate coincidence (one timeout out of ten seeds), not behavioral coincidence (different timeout seed). Evidence in `docs/k3-5c-scaled-train-eval-falsification-evidence.md`. Tests: 9 new unit tests in `tests/rl/test_reward_scaling.py`, broader sweep across `test_rl_config`, `test_cartpole_smoke`, `test_h2_artifacts`, `test_h2_evaluate_smoke`, `test_h2_factories`, `test_h3_train_plumbing`, and `test_reward_scaling` reports 67/67 pass in 11.37 s.

**Next action:** GPT scoping on K4. The K3.5c result decomposes the original eval anomaly into two layers: (1) cross-regime invariance (Phase B/C/D bit-identical across budget and entropy changes), partially broken by K3.5 reward scaling, and (2) within-regime invariance (K3.5c 2048 == K3.5c 10000), still operative. K4 should focus on layer 2: the deterministic-argmax surface is effectively frozen on the panel within a single training regime even with reward scaling preventing value collapse. Recommended K4 diagnostic slice: inspect raw action-net logits on the fixed observation-conditioning panel at update 0, update 8 (K3.5c 2048 boundary), and update 156 (K3.5c 10000 boundary), and check whether the panel argmax distribution actually moves or stays pinned. Candidate mechanisms unchanged from the K3.5b handoff: action-head initialization, CnnPolicy default weight scaling, log-softmax tie-breaking at near-uniform initialization, features-extractor output uniformity producing identical logits across panel obs.

**Blockers:** None requiring Jeff.

**Notes:**

- The bit-identical 2048 vs 10000 K3.5c per-seed eval result with distinct model SHA-256 hashes is the same signature as the original Phase K anomaly, now reproduced INSIDE the K3.5 regime. The original anomaly was "different training recipes -> identical eval"; the within-regime version is "different training budgets -> identical eval." Different cause for the cross-regime layer (now resolved), but apparently the same mechanism for the within-regime layer (still open and now sharper).
- K3.5c aggregate per-seed lengths (`1383, 483, 1293, 603, 1443, 363, 573, 273, 1800, 243`) have no obvious mapping to the Phase D lengths (`903, 1800, 273, 363, 753, 1203, 183, 693, 423, 303`). The K3.5c agent reaches mean length 845.7 vs Phase D 689.7, so K3.5 reward scaling is genuinely changing the trained-policy behavior at the population level, not just shuffling outcomes.
- Background-launch infrastructure finding: `start "" /B cmd /c bat-with-stdout-redirect` on Windows 11 26200 produced a hung Python child (18 MB WS, 0.16 CPU s over 3+ minutes) when stdout was redirected via cmd `>>`. The K3.5c 10000-ts run was launched via PowerShell `Start-Process -RedirectStandardOutput -RedirectStandardError -NoNewWindow -PassThru` which worked cleanly. Update the bat-sentinel memory accordingly: prefer the PowerShell Start-Process pattern for long-running Python jobs that need stdout capture.
- The `SIGHT_TCP_IGNORE_DEATH` refusal guard was not exercised in this session and remains intact in `run_p3_eval.py`. Production train.py exit code 0 on success; no death-tolerant branch needed for the K3.5c flow.
- Files touched: `src/sight_agent/rl/reward_scaling.py` (new), `src/sight_agent/rl/train.py` (CLI flag, train-env wrap, metadata), `src/sight_agent/rl/config.py` (override threading), `tests/rl/test_reward_scaling.py` (new), `docs/k3-5c-scaled-train-eval-falsification-evidence.md` (new), `docs/sight-handoff.md` (this). No changes to `src/sight_agent/rl/h5_baseline_cli.py`, `games/`, or `tools/h5_training_entropy_probe.py`. The h5 eval CLI's existing `--mode full --policies trained_cnn --train-run-dir` path was used unchanged.
