# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K K2 baseline shared-head train-seed asymmetry probe complete. K2 verdict K2-A: all four shared-head seeds tested (2 from K0-10k, 1/3/4 from K2) wedge to verdict K-A within 10k timesteps. The K0-10k wedge is architectural under this evidence, not a seed 2 artifact. Secondary K2-C-flavored result: basin action is seed-dependent (seed 1 wedges to `stay`; seeds 2/3/4 wedge to `left`; no seed wedged to `right`). Phase nomenclature unchanged (H5 amended).

**Last commit:** `__SUBSTANTIVE_HASH__` Phase K K2 baseline shared-head train-seed asymmetry probe: seeds 1/3/4, verdict K2-A.

**Current task:** K2 baseline shared-head probe ran three instrumented 10000-timestep training sessions in series via `C:\Users\maste\AppData\Local\Temp\sight_k2\run_k2_serial.bat` (config `configs\rl\signal_dodge_ppo_h5_pixel_entropy.yaml`, 40 PPO updates per seed, wall times 311.9 / 312.4 / 312.7 s for seeds 1 / 3 / 4). All three crossed the collapse classifier: seed 1 entropy_first=8 / margin_first=8 / top_action_first=9 to `stay` basin; seed 3 entropy_first=20 / margin_first=20 / top_action_first=21 to `left` basin; seed 4 entropy_first=13 / margin_first=13 / top_action_first=14 to `left` basin. K2 falsifies both the "K0-10k wedge is seed 2-specific" hypothesis and the "left-wall basin" framing. K0-10k seed 2 turns out to be the *mildest* shared-head wedge of the four tested (max margin 3.62 vs K2 seeds 8.15-13.07). Evidence appended at `docs/h5-phase-k-training-entropy-probe-evidence.md` sections 25-31. Artifacts gitignored under `runs/phase_k/` with sha256s captured in section 26 sub-blocks.

**Next action:** GPT to scope the next slice. K2 has reordered the priority space documented in the evidence section 31. Recommended next architectural probe is a **value-head capacity sweep** (for example `policy_kwargs.net_arch = dict(pi=[64], vf=[128])` or `vf=[256]`, with shared-head retained where possible) to find the minimum value capacity that enables observation-conditional commitment rather than constant-action collapse. K1-extended (rerun K1 at 30k or 50k) remains parked and is now lower priority than the capacity sweep because K2 makes the architectural mechanism more central than the training-budget mechanism. A short investigative slice on initial weight init plus first-rollout action statistics could close the secondary "why does seed 1 pick stay while seeds 2/3/4 pick left" question without further training. Routine execution of any of these does not require a Jeff go signal under the corrected governance; scope/ethics/legal/IP/target-environment changes do.

**Blockers:** None.

**Notes:**

- K2 four-seed result strengthens the architectural-mechanism reading of K1. The shared-head wedge is not a seed artifact and not a left-wall artifact; it is a constant-action attractor under CnnPolicy + entropy recipe + 10k timesteps. Seed determines *which* action becomes constant.
- K0-10k seed 2 is mild relative to the other shared-head seeds (final entropy 0.82 vs ~0.00-0.01; max margin 3.62 vs 8.15-13.07; mean L=0.66 vs ~1.0 for seeds 3/4). The K0 narrative would be sharper anchored on seed 3 or seed 4 as canonical references. Demo-0 currently visualizes the mildest shared-head wedge.
- K2 launch pattern: serial bat-with-sentinel at `C:\Users\maste\AppData\Local\Temp\sight_k2\run_k2_serial.bat` with `SIGHT_GODOT_EXE` inline, three seeds in one process, one done sentinel at the end recording `seed1=<rc> seed3=<rc> seed4=<rc>`. Polled via `ping -n N 127.0.0.1 > nul` from a cmd-shell `interact_with_process`. Note that `timeout /t N /nobreak > nul` causes Desktop Commander's interact_with_process to exit the shell with "Input redirection is not supported"; `ping` is the safer delay primitive.
- Honest read: K1's mechanism-intervention success (no wedge under split 64/64 heads) plus K1's value-learning failure (EV stuck at zero), now combined with K2's confirmation that shared-head wedges across seeds, strongly suggests the next architectural variable to manipulate is value-head capacity specifically, not policy-head capacity and not training budget.
- Demo-0 chain still applies: `tools/demo0_visible_play.py` is the deployment-time evidence tool. A K2 seed produces a `model.zip` only if the entropy probe writes one, which it does not (probe is diagnostic-only). The first non-wedge converged policy from a future capacity-sweep slice plus Demo-0 against eval seed 1008 remains the visible-learning deliverable.
