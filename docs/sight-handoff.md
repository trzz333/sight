# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K (K4.1 eval-observation panel-logit mechanism diagnostic complete. Verdict **K4.1-B boundary-equivalent drift** on the entire reached eval distribution. 10 seeds (1000-1009), 8457 captured eval steps, episode lengths 243-1800 reproducing the K3.5c claim exactly. Cross-probe argmax match between K3.5c 2048 and K3.5c 10000 is 8457/8457 = 1.000000. Both checkpoints output action "left" on 100% of all 8457 reached eval observations. Pass 1 (actor K3.5c 10000) and Pass 2 (actor K3.5c 2048) achieve bit-identical trajectories: length 10/10, termination 10/10, per-step obs SHA-256 8457/8457, per-step action 8457/8457. Logit drift between checkpoints is material: L_inf max 0.2364 (above the 0.1 threshold), L1 max 0.5427, mean margin grows from 0.287 at 2048 to 0.374 at 10000. The smallest top1-top2 margin observed on the eval distribution is 0.2834, comfortably above the largest inter-model logit shift of 0.236; the decision boundary survives the drift with ~5% buffer at the worst point. K4.1-E reproducibility cleared. K3.5c bit-identical eval is mechanistically explained: the K3.5c policy is a degenerate constant-left policy on its reachable eval distribution, training between updates 8 and 156 sharpens the "left" preference without ever crossing a decision boundary, and trajectories coincide step by step because every reached eval obs maps to the same action on both checkpoints).

**Last commit:** `1ba860f` Phase K K4.1 eval-observation panel-logit mechanism diagnostic: K4.1-B boundary-equivalent drift, full eval distribution (substantive). Chore handoff refresh follows in this push.

**Current task:** K4.1 eval-observation panel-logit mechanism diagnostic per the GPT K4.1 scope. Built `tools/k4_1_eval_obs_logit_probe.py` (~605 lines) that reuses the standard Godot eval factory path (`sight_agent.rl.factories.make_env(env_id, n_envs=1, seed=seed, mode="eval", run_dir=..., **godot_extra)`) and runs two passes over seeds 1000-1009. Pass 1: actor K3.5c 10000 drives deterministic-argmax rollout, capturing per-step obs, obs SHA-256, K3.5c 10000 self-probe (logits/probs/argmax/margin), and K3.5c 2048 cross-probe forward pass on the same obs tensor. Pass 2: actor K3.5c 2048 drives rollout, capturing only obs SHA-256 and actions for trajectory parity verification against Pass 1. Compared trajectories per seed for length / termination / per-step obs hash / per-step action; aggregated logit drift over the 8457 Pass 1 rows; classified to K4.1-B. Artifacts: `runs/phase_k/k4_1_eval_obs_logit_probe.json` (~17 KB), `runs/phase_k/k4_1_eval_obs_logit_probe_rows.csv` (~3.3 MB, 8458 rows incl. header), `docs/k4-1-eval-obs-logit-mechanism-evidence.md` (311 lines). End-to-end wall time ~470 s (Pass 1 ~235 s, Pass 2 ~235 s). Background launch via the same PowerShell `Start-Process -Wait -RedirectStandardOutput -RedirectStandardError -NoNewWindow -PassThru` pattern validated in K4.0; survives the 4-minute MCP `interact_with_process` ceiling without issue.

**Next action:** GPT scoping on K5. The K3.5c "bit-identical eval despite distinct weights" anomaly is mechanistically resolved (K4.1-B). The new load-bearing question is the constant-left attractor itself: does the K3.5c training regime always land here, or is it a seed-specific accident? Recommended K5.0 experiment: train H5 entropy config (10000 timesteps, K3.5 reward scaling /30) at seeds 1, 2, 3, 4, 5 and rerun K4.1-style eval-obs probe on each. Tests whether (a) every seed lands in a constant-action attractor (yes / no), (b) the dominant action is always "left" or varies by seed (informs the early-trajectory-accident vs spawn-asymmetry vs argmax-init-lottery candidates from K4.0), (c) whether any seed reaches an obs with top1-top2 margin smaller than ~0.28 (the K3.5c floor) that would be flippable under further training. If K5.0 confirms a consistent constant-action attractor across seeds, the research question shifts from "why are eval results bit-identical" (answered) to "what training intervention escapes the constant-action attractor on Signal Dodge" and reward shaping, architecture change, and curriculum become candidates again for K5.1.

**Blockers:** None requiring Jeff.

**Notes:**

- The mechanistic answer to the K3.5c anomaly is now: K3.5c 2048 and K3.5c 10000 have distinct SHA-256 (E14D1A12... vs 5664A12E...), distinct action_net weights (row norms grew {L=0.086, S=0.045, R=0.083} at 2048 to {L=0.112, S=0.116, R=0.104} at 10000), and distinct logits on every eval obs (L_inf max 0.236, L1 max 0.543), but both checkpoints output action "left" deterministically on every one of 8457 reached eval observations. Trajectories coincide because the argmax surface coincides on the reached distribution. Eval invariance is downstream of policy degeneracy, not of a checkpoint-loading bug or eval-pipeline defect.
- K4.0's K4-D verdict on the narrow scripted panel now generalizes to the full reached eval distribution. K4.0's K4-A (feature-extractor uniformity) and K4-F (init-surface persistence) hypotheses remain falsified; K4.1 confirms K4-C-style action-head pinning extends across all 8457 reached eval observations on both checkpoints.
- The K3.5c policy is constant-left on its reachable eval distribution under deterministic-argmax eval. 9/10 seeds terminate in collision, 1/10 (seed=1008) reaches the 1800-step timeout. This matches the wall_hugging_into_collision classification used by the Phase J stochastic-eval ablation tool. The K3.5 reward-scaling correction did not move the policy off the constant-action attractor; K-phase exit requires a different lever (seed sweep, architecture, curriculum, or shaped reward), not more training steps at the current recipe.
- The smallest top1-top2 margin observed on the entire 8457-row eval distribution is 0.2834 (K3.5c 2048 floor). The largest inter-model L_inf logit shift over 7952 training timesteps is 0.236. A training regime that produces logit shifts of magnitude >= 0.28 on the eval distribution would push some obs across the "left vs stay" boundary and demonstrate behavior change; K3.5c training between 2048 and 10000 falls narrowly below this bar.
- The `SIGHT_TCP_IGNORE_DEATH` refusal guard was not exercised in this session and remains intact in `scripts/run_p3_eval.py`. K4.1 does not run training and does not depend on the death-tolerant branch.
- Background-launch pattern unchanged from K4.0 and continues to work cleanly: wrapper `.ps1` with `Start-Process -Wait -RedirectStandardOutput -RedirectStandardError -NoNewWindow -PassThru` for the Python child plus a done-sentinel write, launched from cmd via `powershell -NoProfile -Command "Start-Process powershell -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File','<path>' -NoNewWindow -PassThru"`. K4.1 wall time ~470 s, well past the 4-minute MCP `interact_with_process` ceiling.
