# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** VZD-3 deadly_corridor. Skill-3 stage 2 PASSED and combat-verified. Skill-5 resume-finetune LIVE under Task Scheduler.

**Last commit:** fb7ff99 vzd: resolve fight-or-run-past (FIGHT), launch skill-5 finetune

**Current task:** The skill-5 resume-finetune is running as scheduled task `Sight-VZD3-S5` (supervisor pid was 27936 at launch, target 3.0M steps, ~1.5M new skill-5 steps at ~116 fps, so ~3.6h wall from 2026-07-18 19:52). Out dir `runs\vzd\ppo_deadly_corridor_s5_ft`, seeded from the skill-3 `ppo_deadly_corridor_1500000_steps.zip` + step-matched `vecnormalize_1500000_steps.pkl`. Early telemetry is healthy and doing what the design intends: entropy_loss is climbing from the checkpoint's -0.08 (at ~1.52M steps -0.384, at ~1.64M steps -1.46; the raised `--ent-coef 0.05` re-opened exploration on the near-deterministic policy), approx_kl and clip_fraction nonzero (not frozen), value_loss ~0.2-0.5 (VecNormalize stats restored, critic on normalized scale), explained_variance low/negative (value fn re-fitting the harder skill-5 return distribution). This is a finetune re-adapting, not coasting.

**Fight-or-run-past is RESOLVED: FIGHT (HIGH).** The skill-3 shaped+norm policy kills, it does not sprint past. 30-ep deterministic raw eval instrumented with engine counters (`tools\vzd_probe_combat.py`, `runs\vzd\ppo_deadly_corridor_s3_shaped\combat_probe.json`): 5 kills / 5 hits / 0 deaths every episode, mean final health 91.4. Tic-level trace (`tools\vzd_trace_combat.py`, `runs\vzd\_trace_combat.log`): every KILLCOUNT increment coincides with a HITCOUNT increment and a -1 on AMMO2, one bullet one kill five times (killed my own intermediate infighting hypothesis). Probe reproduced `summary.json` to 4 decimals, re-verifying stage 2. A skill-3 demo clip exists at `runs\vzd\demos\corridor_s3_shaped.mp4` (40s, 640x480), though its mtime (Jul 16) predates this session's probe write, so treat provenance as unverified and re-capture if it is going on the reel. Findings section 4a updated.

**Next action:** When `Sight-VZD3-S5` finishes (DONE sentinel in the out dir, or `supervisor.log` shows "trainer exited clean"), read `runs\vzd\ppo_deadly_corridor_s5_ft\summary.json`. Pre-registered bar: raw skill-5 IQM decisively above the flat skill-5 baseline IQM 93.6 (the sprint-and-die collapse). The measured *starting* point for this finetune is IQM ~93.3 (the skill-3 policy evaluated cold at skill 5, from the smoke run), so improvement over ~93 is the transfer signal. Then run the SAME combat probe at skill 5 (`tools\vzd_probe_combat.py --doom-skill 5`) to confirm it still fights rather than reverting to sprint-and-die, and capture a skill-5 demo clip. If skill 5 passes and fights: VZD-3 is done, record the results-table row (findings section 5) and cut the public demo reel. If it fails or reverts to run-past: escalate per findings section 6 recommendation order (vf_coef 0.5->0.25, then further ent-coef, then share_features_extractor=False), or go to intrinsic exploration (RND/ICM, section 2) rather than retrying the curriculum harder (method-fails-twice).

**Blockers:** None requiring Jeff.

**Notes:**

- **Long jobs are Task Scheduler tasks, never detached children.** New task this session: `Sight-VZD3-S5` -> `runs\vzd\_run_s5_ft_task.cmd`. Existing: `Sight-VZD3` (s3, done), `Sight-Monitor` (`schtasks /Run /TN Sight-Monitor`, http://127.0.0.1:8791/monitor.html). Detached children died 3x training + 3x monitor; deleting a task kills its running instance. `/RL HIGHEST` and `/SC ONLOGON` need elevation and were dropped; `/SC ONCE /ST 23:59` + manual `/Run` is the working pattern.
- **`--ent-coef` was a silent no-op on `--resume` before this session (now fixed, `fb7ff99`).** `PPO.load` restores the checkpoint's saved `ent_coef`, so the CLI flag was ignored while still being written into `summary.json` (a false report). `vzd_ppo_train.py` now reapplies it after load. Verified `self.ent_coef` is read as a plain float at `ppo.py:256` (SB3 lives at `C:\Users\maste\AppData\Roaming\Python\Python314\site-packages`, NOT `.venv-c1\Lib`); smoke-confirmed `summary.json` records 0.05 and the resume prints the override.
- **Seed a resume dir from the numbered checkpoint, never `model.zip`.** `_sibling_vecnormalize()` matches `_<N>_steps.zip` -> `_vecnormalize_<N>_steps.pkl`; `model.zip` matches nothing, so resuming from it re-estimates the return std from 1.0 and re-inflates the returns whose scale caused the section-3a entropy collapse (undoing the fix it protects). The supervisor's `newest_ckpt` scans the out dir, so the seed checkpoint must be present and correctly named or the run trains from scratch.
- **`combat_probe.json` SHOTS_FIRED/accuracy are UNRELIABLE** (AMMO2 baseline read at reset() inherits the prior episode's terminal value -> impossible accuracy 1.007). KILLCOUNT/HITCOUNT/DAMAGE_TAKEN are clean. The tic-level trace is the trustworthy accuracy source (5/5 to 5/6). If accuracy is needed at scale, fix the baseline read to sample AMMO2 on the first post-reset step, not in reset().
- **Root cause of the runs-2/3 mass kill is still UNKNOWN**; Task Scheduler as the fix is MEDIUM (n=1), not HIGH. The 3db4777 "ViZDoom engine death" diagnosis was WRONG (findings section 8). If a long job dies again, read `supervisor.log` + `SUP_HEARTBEAT` first: a stalled heartbeat with no "leg N exited rc=" line proves killed, not crashed. Kept regardless: SafeDoom, 50k checkpoints with `save_vecnormalize=True`, the supervisor.
- **Standing caveats:** stage-2 skill-3 pass is a SINGLE SEED (n=1); do not over-read one clear, seeds are owed before any decisive transfer claim. vizdoom 1.3.0, sb3 2.8.0, Python 3.14. Eval is deliberately RAW so numbers compare across runs; shaped ep_rew_mean is NOT comparable to the flat curve. `runs\` is gitignored.
