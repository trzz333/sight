# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K K1 architecture mechanism probe complete. K1 verdict K-C: the K0-10k wedge does not reproduce under `policy_kwargs.net_arch = dict(pi=[64], vf=[64])`. Mechanism intervention successful but inconclusive about competence (value head shows no learning signal inside the 10k budget). Phase nomenclature unchanged (H5 amended).

**Last commit:** `336f6da` Phase K K1 architecture mechanism probe: net_arch pi/vf=[64]/[64], verdict K-C.

**Current task:** K1 architecture probe ran one instrumented 10000-timestep training session (train_seed=2, `configs/rl/signal_dodge_ppo_h5_pixel_entropy_k1_netarch.yaml`, 40 PPO updates, 311.5 s wall) via the digest-patched `tools/h5_training_entropy_probe.py`. No collapse threshold crossed: entropy floor 0.96, sampled top-action fraction ceiling 0.547, raw margin ceiling 0.66. Deterministic argmax oscillated across updates rather than locking: `left` upd 1-7, `right` upd 8-19, `left` upd 25-38, `right` upd 39-40. EV never crossed zero. K1 demonstrates the K0-10k wedge is architecture-dependent; it does not demonstrate K1 produces a learning policy. Evidence appended at `docs/h5-phase-k-training-entropy-probe-evidence.md` sections 18-24. Artifact sha256: ndjson `5e8cb430…`, summary `48f24a71…` (gitignored under `runs/phase_k/`). Tool patch: `tools/h5_training_entropy_probe.py` `per_update_digest` now surfaces `det_argmax_pre`, `det_argmax_fraction_pre`, `det_argmax_post`, `det_argmax_fraction_post`, `policy_prob_left_post`, `policy_prob_stay_post`, `policy_prob_right_post`. Sibling config: `configs/rl/signal_dodge_ppo_h5_pixel_entropy_k1_netarch.yaml`.

**Next action:** GPT to scope the next slice. Two honest forks: **K2 train-seed asymmetry** (rerun K0-10k baseline at train_seed in {1, 3, 4} with shared-head architecture to test whether the K0-10k wedge is seed-specific or architectural) is structurally more informative because it disambiguates "architecture matters" from "seed matters" before further architecture changes. **K1-extended** (rerun K1 at `total_timesteps=30000` or `50000`) is cheaper and answers whether the K1 value head ever learns. K2 is the recommended next slice; K1-extended is a useful parallel if Jeff wants both. Routine execution of either does not require a Jeff go signal under the corrected governance (scope/ethics/legal/target-environment changes do; mechanism slices do not).

**Blockers:** None.

**Notes:**

- Governance correction this session: the phrase "pending explicit go signal" is removed from the K-series ladder framing. Routine mechanism probes execute on technical merit. Jeff decisions are reserved for scope, ethics, legal, IP, supervision, money, target environments, and mom-facing comms.
- `policy_kwargs.net_arch` in YAML hyperparams flows cleanly through `sight_agent.rl.config.load_config` (validation only requires `algo.hyperparams` is a mapping; no key restrictions) and `tools/h5_training_entropy_probe.py` (line 765: `hyperparams = dict(algo_cfg.get("hyperparams") or {})` → splatted into `InstrumentedPPO(**hyperparams)`). SB3 PPO accepts `policy_kwargs={"net_arch": {"pi": [64], "vf": [64]}}` directly on CnnPolicy; the MLP heads sit on top of the shared NatureCNN feature extractor.
- K1 launch pattern: bat-with-sentinel at `C:\Users\maste\AppData\Local\Temp\sight_k1\run_k1.bat` with `SIGHT_GODOT_EXE` inline, stdout/stderr to log, ERRORLEVEL written to done sentinel. Launched detached with `start "k1_probe" /MIN cmd /c <bat>`. Polled sentinel via PowerShell `Test-Path` from the interactive shell. K1 wall (311.5 s) is essentially identical to K0-10k (312.0 s), confirming the pi/vf MLP heads add negligible compute over the shared head.
- Honest read: K1 is a "weak win + different-basin" result per GPT's prior decision rule. Architecture changes the mechanism but does not yet produce observation-conditioned learning. The basin oscillation (left→right→left→right across updates) plus EV stuck at zero suggests the 64-unit value MLP is too small to learn returns in 10k timesteps. K2 should be prioritized over more K1 variants to test the underlying assumption that the wedge is architectural rather than seed-specific.
- Demo-0 chain still applies: `tools/demo0_visible_play.py` is the deployment-time evidence tool. Once a non-wedge converged policy exists (after K1-extended or after K2 produces a basin-stable policy with a checkpoint), running Demo-0 against eval seed 1008 with that policy is the visibility deliverable. K1 itself writes no `model.zip` (probe is diagnostic-only).
