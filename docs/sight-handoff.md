# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** VZD-3 COMPLETE. Two-seed replication done: both seeds passed both stages (s3 and s5) and both are combat-verified FIGHT. Next phase is the results write-up and portfolio packaging. Machine: MSI Raider 18 HX (hostname MSI, RTX 4080 Laptop 12GB); StrongerJr retired.

**Last commit:** 5088306 infra: vizdoom window hider daemon + rect-logging window watcher

**Current task:** VZD-3 is finished and the machine is idle. Seed-1 s5 finetune finished clean at 3.0M steps: raw skill-5 IQM 2280.44 vs bar 93.6; combat probe FIGHT (5.9 kills/ep, 30/30 episodes with kills, 28/30 survived; probe IQM matches training eval exactly, same deterministic eval). The probe had crashed for 17h of half-hourly refires because the chained cmd omitted --env-id, defaulting to VizdoomDefendCenter-v1 whose Discrete(4) action space rejects the corridor policy's actions; that one line was also the source of the recurring vizdoom popup windows. With it fixed, seed-2 ran the ENTIRE pipeline unattended: s3 shaped leg IQM 2277.95 vs bar 683.9, auto-probe FIGHT (5.0 kills/ep, 30/30 survived); s5 finetune resumed from the 1.5M checkpoint, finished clean at 3.0M, IQM 2279.69, auto-probe FIGHT (5.77 kills/ep, 28/30 survived). All three final IQMs sit in 2277.95-2280.44. Skill-5 policies genuinely fight; the distance-only caveat is closed for both seeds. The doom window hider daemon is deployed (see Notes). Monitor incident 7/22: during duplicate cleanup the live serve_monitor listener was killed (verification had run before the kill); restarted via Sight-Monitor task, re-verified HTTP-vs-disk byte-equal after. Scheduled tasks now: Sight-Monitor, Sight-DoomHider only; both VZD3 training tasks deleted.

**Next action:** Draft the VZD-3 results section: update docs\vzd-deadly-corridor-findings.md and the repo README with the two-seed results table (s3/s5 IQM vs pre-registered bars, FIGHT probe kill and survival numbers; omit accuracy, SHOTS_FIRED is flagged unreliable) and the curriculum-transfer narrative, then record a gameplay clip with tools\vzd_probe_combat.py --record on the seed-1 s5 model for the portfolio. At any future run launch, hand Jeff the monitor URL in the same message as the launch report, first thing.

**Blockers:** None requiring Jeff. (Optional, unchanged: LAN monitor access from other devices needs an admin inbound rule for 8791; one-liner given in session log. On-box URL http://MSI:8791/monitor_s5_seed2.html serves the finished run.)

**Notes:**

- **VZD-3 ledger:** seed-1 s3 IQM 2276.58, s5 2280.44; seed-2 s3 2277.95, s5 2279.69. Bars 683.9 (s3) and 93.6 (s5). Four FIGHT probes, kill counts 5.0-5.9/ep, survival 28-30 of 30. Curriculum (s3 shaped 1.5M then s5 resume-finetune to 3.0M) reproduced start to finish on a second seed with zero manual intervention. README transfer claim is licensed.
- **Doom hider daemon** (tools\doom_window_hider.py, hourly self-heal task Sight-DoomHider, Startup vbs): pid-scoped WinEvent hooks + 50ms sweep, empty-region park. Prevention approaches are dead ends, enumerated in commit 5088306. UNKNOWN x2, instrumented not solved: owner process died silently twice (~minutes lifetime; traceback logging added and NO crash logged since, so death is not a Python exception, suspect external kill); one of any pair of simultaneously launched venv pythons wedges at interpreter boot (seen in hider, supervisor, and train duplicates; ~0 CPU, inert; SUP_LOCK raced past once with a wedged benign duplicate). Check runs\vzd\doom_hider.log on any popup report, then window_watch.log (now logs window rects).
- **Process-kill rule (from the 7/22 monitor incident):** before killing any "duplicate" in a family that owns a port, check socket ownership with Get-NetTCPConnection; low CPU does not identify the dead twin. And verify AFTER mutating, never before.
- **Monitor discipline:** the URL goes to Jeff at every launch, no exception; missed twice this session and Jeff caught it. Serving verification is HTTP-vs-disk byte equality, never status 200.
- **Standing caveats carried:** --ent-coef must be reapplied post PPO.load; resumes seed from numbered _<N>_steps.zip + step-matched VecNormalize pkl, never model.zip; combat_probe SHOTS_FIRED/accuracy UNRELIABLE, KILLCOUNT/HITCOUNT/DAMAGE_TAKEN clean; eval is RAW and cross-run comparable; DC waits under ~50s, long jobs detached via pythonw launch_hidden and verified by output files; vizdoom 1.3.0, sb3 2.8.0, Python 3.14; runs\ gitignored.
