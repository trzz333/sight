# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. K5.7 (QR-DQN + n-step, epsilon-greedy) 200k final eval STILL PENDING (run detached, not finished). K5.8 (NoisyNet QR-DQN) STAGED and unit-verified but NOT yet trained or smoke-tested against live Godot. K5.9 (DQfD-lite, demonstration-seeded replay) is the named high-confidence fallback if K5.8 also collapses. From-scratch RL must beat the 930.27 constant-action baseline. PPO rejected K5.1-K5.5; PPO finetune rejected K5.6; vanilla DQN rejected (right-collapse); QR-DQN 50k ckpt FAIL (left-collapse). Single-voice governance.

**Last commit:** `14faf6f` K5.8 NoisyNet QR-DQN staged; env proven L/R symmetric; K5.7 200k still running

**Current task:** K5.7 200k QR-DQN run is STILL RUNNING detached and unfinished at session close: sentinel runs\phase_k\k5_7_qrdqn_200k.sentinel ABSENT, last observed ~102.8k/200k steps, loss climbing monotonically (504 -> 580 over the session), in-training ep_len_mean ~497, exploration_rate floored at 0.05. The K5.7 final in-env eval (the prior Next action) therefore did NOT run; there is no completed 200k model to eval. The 50k checkpoint remains FAIL (mean 736.3, frac_R 0.000, left-collapse), anchored to runs\phase_k\k5_7_qrdqn_ckpt50k\eval_inenv\dqn_eval_inenv_report.json. This session used the CPU-starved window to verify a structural finding and stage the next escalation. Structural finding (HIGH, anchored to games\signal-dodge\scripts\main.gd / player.gd / hazard.gd this session): the env is provably left/right SYMMETRIC (player starts centered, symmetric speed 300 and bounds, hazard x uniform over full width, observation antisymmetric under reflection). So the cross-method single-direction collapse (PPO constant, DQN right-only, QR-DQN left-only) is an EXPLORATION pathology, NOT an env asymmetry. CRITICAL reframe (this session): the task is EASY (CartPole-tier dodge, dense +1/step reward) and a good policy provably exists in the same network on the same observation because BC hit 1737.3 through the exact same env path. So the from-scratch collapse is a mis-wired RL setup, NOT a fundamental wall. found-art (web search: DQfD demonstration-seeded replay; DQN constant-action collapse; CleanRL Rainbow/NoisyNet): DQfD (Hester 2018) prefills the replay buffer with demonstrations and keeps them, the textbook cure for value-RL collapse when a good policy demonstrably exists. K5.8 NoisyNet (Fortunato 2017 factorised noise, epsilon disabled) unit-verified TWICE against on-disk code: k5_8_noisy_selftest.py PASS (3 noisy layers, train_diff 0.206 noise-live, eval_diff 0.0 deterministic, learn ran); k5_8_loadtest.py PASS (save/reload predicts identically and deterministically). NOT tested: K5.8 against live Godot (a smoke run), because the 200k owns the i7-10750H.

**Next action:** Run C:\Projects\Sight\tools\k5_poll.py. If sentinel DONE, set SIGHT_GODOT_EXE inline then eval K5.7 final: python tools\k5_7_qrdqn_eval_inenv.py --run runs\phase_k\k5_7_qrdqn --seeds 1000-1009 (PASS only if mean >= 930.27 AND frac_L >= 0.03 AND frac_R >= 0.03 AND max(frac) < 0.97). Expect FAIL or degenerate. On K5.7 FAIL, do NOT retry QR-DQN; walk the escalation ladder in the notes. Step 1: smoke-test K5.8 NoisyNet (set SIGHT_GODOT_EXE inline, python tools\k5_8_noisy_qrdqn_train.py --timesteps 8000 --seed 0 --out runs\phase_k\k5_8_smoke, confirm exit 0 and finite ep_len_mean), then if clean launch the full 200k detached via .bat (python -u, --timesteps 200000 --out runs\phase_k\k5_8_noisy_qrdqn, .log redirect, echo DONE %ERRORLEVEL% > .sentinel) and eval with tools\k5_8_noisy_eval_inenv.py. If K5.8 in-env eval FAILs, build K5.9 DQfD-lite (prefill the SB3 replay buffer with oracle/BC demonstration transitions, fixed expert:agent sampling ratio, no margin loss needed for the lite variant). Run the cheap reward-scale/value-stability check (note 1c) any time it is convenient, in parallel. If the 200k is still running on resume, lean-poll (sleeps <= 45s); do not launch anything while it holds the CPU. Wrap git invocations in cmd /c "..." (raw shell rejected git commit -F twice this session).

**Blockers:** None requiring Jeff. Scope decision is informed and defaulted, not blocking (see note 5). Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable.

**Notes:**

- Escalation ladder after K5.7 FAIL, in order. (a) K5.8 NoisyNet QR-DQN: staged, purity-preserving (zero demonstrations), changes the exploration MECHANISM not the epsilon constant. MEDIUM it clears the bar. (b) K5.9 DQfD-lite: prefill SB3 replay buffer with oracle/BC demonstrations + fixed expert:agent sampling ratio (Hester 2018; the textbook cure for value-RL collapse when a good policy provably exists, which it does here). MEDIUM-HIGH, lowest risk, reuses assets already on disk. (c) reward-scale/value-stability check: loss climbing monotonically to 500+ on 1800-step gamma 0.99 episodes smells like value-target magnitude instability; test reward scaling (e.g. 0.01/step) or return normalization. LOW-MEDIUM but cheap, and if it is the root cause even stock DQN starts working. Do not retry QR-DQN harder.
- K5.8 staging is committed but UNPROVEN in-env. The unit tests prove the noise mechanism and the save/load path, NOT that NoisyNet escapes the basin. That verdict comes only from the in-env greedy eval on seeds 1000-1009 after a real 200k train. Do not claim K5.8 works until that eval PASSes.
- The 200k QR-DQN run saturates the i7-10750H; the ndjson callback rewrites the whole file each rollout (truncate-then-write), so a naive reader can hit an empty file. k5_poll.py retries the read. The run is detached and survives poll timeouts.
- Cross-method collapse is the central wall, now read as exploration collapse in a symmetric env (NOT an env or value-overestimation problem). The only above-baseline policies remain BC (1737.3) and PPO-finetune-from-BC (1710.5), both warm-started. The honest from-scratch-RL demo still does not exist.
- SCOPE (Jeff-owned, informed this session, defaulted not blocking): "from scratch with ZERO demonstrations" is a handoff-only purity bar, NOT in the charter; the charter mission (a learned policy beating baseline on Signal Dodge) is already met by BC. K5.9 DQfD-lite satisfies the charter but not the no-demos purity bar; K5.8 NoisyNet satisfies both. Default: try purity first (K5.8), DQfD-lite authorized as fallback. reward_shaping "none" stays load-bearing (best constant 845.7 < bar 930.27); runs/ is gitignored.
