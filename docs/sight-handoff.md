# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** H5 amended; Phase K K0 training-time entropy-collapse probe complete. K0 ran one instrumented training session (2048 timesteps, 8 PPO updates, train_seed=2, config `configs/rl/signal_dodge_ppo_h5_pixel_entropy.yaml`) with `tools/h5_training_entropy_probe.py` (new). Subclasses SB3 PPO and overrides `train()` mirroring SB3 2.8.0 body exactly, adding per-minibatch grad norms, pre/post-update entropy and raw-logit margin on same rollout obs, advantage/return/value stats, action_net weight/bias deltas, K-A/K-B/K-C/K-D auto-classification. Wall time 64.79 s for 8 PPO updates (16 minibatches per update = 128 total minibatches).

Headline findings. Verdict K-C: none of the three collapse thresholds crossed across all 8 PPO updates. Mean policy entropy stays in `[1.025, 1.083]` against `ln(3) = 1.0986` ceiling (93-99% of uniform max). Rollout top-action sampling fraction stays in `[0.395, 0.523]` well below 0.95. Mean raw top1-top2 margin oscillates in `[0.082, 0.599]` with no growth trend toward 4.0; ends at 0.116 by update 8. Advantage std healthy (1.25 to 12.59). Explained variance near zero throughout (-0.008 to 0.044), normal for fresh value head at small budget. Action_net row norms grew from uniform 0.010 to {left 0.0140, stay 0.0151, right 0.0132}, reaching 55-74% of Phase E seed 2's final magnitudes (Phase I section 10: left 0.0247, stay 0.0273, right 0.0178).

Two qualitative findings beyond the verdict. First, at 2048 timesteps the trained margin (0.12) is 14% of Phase E seed 2's converged margin (0.83, Phase I section 8) so the wedge-commitment basin Phase J seed-1008 falsified does not exist at this budget; it forms between 2048 and 10000 timesteps. Second, K0 rollout argmax oscillates between left (updates 1, 2, 7, 8) and stay (updates 3-6); the Class B identity "train_seed=2 picks left" is not yet established. Whether the basin assigns to `left` or `stay` is undecided at 2048 timesteps.

K-B precedence rule has a minor scoping issue: the OR semantics on advantage-std-or-explained-variance would fire on every early training run because explained_variance is normally near zero in the first ~2k timesteps. K-B never engaged in this run because there is no entropy collapse to compare against. Future probe iterations should tighten K-B to AND semantics or to a stricter explained_variance ceiling. Documented in evidence doc section 6.

**Last commit:** TBD this session (substantive + chore pair).

**Current task:** Phase K K0 evidence shipped at `docs/h5-phase-k-training-entropy-probe-evidence.md` (10 sections: TL;DR, why Phase J forces this slice, tool and methodology, run configuration and fingerprints, per-update results, collapse-threshold check, verdict and rationale, recommended next slice, reproduction recipe, implementation-not-at-fault). Tool `tools/h5_training_entropy_probe.py` implements `InstrumentedPPO` subclass. NDJSON at `runs/phase_k/entropy_probe_seed2.ndjson` (8 update records + header), summary JSON at `runs/phase_k/entropy_probe_seed2.summary.json` (all gitignored).

**Next action:** K-C interpretation rule applies. Rerun the same probe to 10000 timesteps (full YAML default) before drawing structural conclusions or moving to K1 architecture probe. Invocation: `python tools\h5_training_entropy_probe.py --config configs\rl\signal_dodge_ppo_h5_pixel_entropy.yaml --seed 2 --total-timesteps 10000 --out-dir runs\phase_k --label entropy_probe_seed2_10k`. Expected: ~39 PPO updates, ~5-6 min wall time. Decision rule after rerun: K-A (late) collapse within updates 9-39 → name mechanism and scope K1 against it; K-D wedge-only → K1 architecture probe with `net_arch=dict(pi=[64], vf=[64])`; K-C persistent → pivot to Phase J option (3) train-seed asymmetry probe with Grok consult. K1 and K2 remain held behind this rerun. Jeff approval not required to execute the rerun since it is the K-C clause pre-agreed in the prior planning round.

**Blockers:** none. Operational note: detached `start /B cmd /c` driver from a Desktop Commander shell does not survive parent-shell exit; use inline `interact_with_process` invocation for sessions where the probe completes in < 5 min, or use `start /MIN` (not `/B`) for longer runs.

**Notes:**

- K0 verdict K-C is the expected outcome at a 2048-timestep pilot gate. Entropy near uniform maximum (1.07 vs 1.0986 ceiling), margin 0.12 (14% of E2 trained 0.83), top-action fraction 0.40 (vs 0.95 threshold). The wedge basin forms after this budget, not within it. The K0 pilot has done its job: confirm the probe instrumentation works and confirm the collapse mechanism is not in the first 8 PPO updates.
- K0 rollout argmax oscillation (left → stay → left across 8 updates) is concrete evidence that the Class A / Class B basin identity Phase H section 6 attributed to train_seed is not yet locked at 2048 timesteps. The lock-in happens later. This is a useful data point for the seed-asymmetry probe option (3) when it eventually runs.
- The pre-clip total gradient norm averages 400-905 per minibatch while `max_grad_norm=0.5` clips heavily. Per-module breakdown shows action_net grad norm 3-12 vs total 400-905, so most of the gradient signal flows through `features_extractor` (the NatureCNN). When 10000-timestep rerun runs, watch whether per-module grad norms shift toward action_net concentration around the wedge-formation window.
- Detached `.bat` driver pattern from prior phases (J0/J1, I) does not work from Desktop Commander shells when the spawning shell exits before the .bat completes. First K0 attempt logged only its start header before the parent shell died, taking the detached cmd with it. Inline invocation in `interact_with_process` succeeded in 64.79s. For runs under ~5 min, inline is more reliable; for longer runs, prefer `start /MIN` over `start /B`.
- `InstrumentedPPO.train()` mirrors SB3 2.8.0 `stable_baselines3/ppo/ppo.py` exactly with three additions: per-minibatch pre-clip grad norms (between `loss.backward()` and `clip_grad_norm_`), pre/post-update policy-state and action_net snapshots, and rollout-buffer aggregate statistics. SB3 logger calls preserved with identical keys so downstream callbacks consuming the logger see no shape change. Mirroring is necessary because callbacks fire only at rollout boundaries and cannot capture grad norms between backward and clip.
