# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** H5 (Phase F code landed; observation contract changed to stacked pixels; smoke verified; full diagnostic train sweep NOT yet run)

**Last commit:** `5b8fa48` feat(rl): h5 phase f frame-stack observation contract

**Current task:** Implemented `env.frame_stack` support via SB3 `VecFrameStack` in pixel mode. Touched `src/sight_agent/rl/factories.py` (new `frame_stack` kwarg on `make_env`, wraps pixel-mode VecEnv with `VecFrameStack(n_stack, channels_order="first")` for both train and eval modes), `src/sight_agent/rl/godot_config.py` (added `frame_stack` to `_OPTIONAL_ENV_PASSTHROUGH_KEYS`), `src/sight_agent/rl/h5_baseline_cli.py` (`build_dummy_vec_env_for_cfg` now wraps the dummy with `VecFrameStack` when `env.frame_stack > 1` so the untrained_cnn baseline policy sees the eval-time observation shape), `src/sight_agent/rl/train.py` (`_godot_smoke_obs_metadata` multiplies channels by `frame_stack` so `run_start.env_smoke.obs_shape` matches the policy-facing tensor shape rather than the raw env shape). Added `configs/rl/signal_dodge_ppo_h5_pixel_frame_stack.yaml` (Phase D entropy recipe + `env.frame_stack: 4`). Added `tests/rl/test_h5_frame_stack.py` (17 tests covering factory wrapping, dummy parity, resolver forwarding, YAML loading, train.py metadata fix). Live smoke `h5_train_phase_f_frame_stack_smoke_seed0_512` (512 timesteps, seed 0) ran to completion: `summary.json status=ok`, saved `model.zip` has `observation_space=Box(0, 255, (4, 84, 84), uint8)`. Smoke obs_shape in `run_start` was `[1, 84, 84]` for the seed-0 run (predates the train.py metadata fix); subsequent runs will report `[4, 84, 84]`. Rerun the full rl suite: 306 passed, 1 inherited failure (`test_handoff_does_not_describe_pre_training_gate_as_four_policy`, pre-existing handoff-precision test that has been failing since Phase D dropped the "non-saturation" word from the handoff body; this current handoff restores it).

**Next action:** GPT confirms Phase F diagnostic train sweep can run. Sequence: `python -m sight_agent.rl.train --config configs/rl/signal_dodge_ppo_h5_pixel_frame_stack.yaml --seed <N> --total-timesteps 10000 --run-id h5_train_phase_f_frame_stack_seed<N>_10k` for N in {1, 2, 3} (Phase E rule: diagnostic-not-selection over seeds 1, 2, 3; seed 0 result stays descriptive only), then `python -m sight_agent.rl.h5_baseline_cli --config configs/rl/signal_dodge_ppo_h5_pixel_frame_stack.yaml --run-id h5_eval_phase_f_frame_stack_seed<N>_10k_trained_only --seeds 1000-1009 --mode full --policies trained_cnn --train-run-dir runs/rl/signal_dodge_ppo_h5_pixel_frame_stack/h5_train_phase_f_frame_stack_seed<N>_10k`. Interpretation rule per amended Phase F outcome table in GPT's prompt: aggregate over seeds {1,2,3}; row 3 of the table (target diagnostic bar `collision_rate <= 0.8` and reward/length not worse than Phase E) determines Phase F success at the diagnostic level. No best-of-N seed selection.

**Blockers:** none operational. Open: Jeff to relay this handoff to GPT for the green light on the diagnostic sweep.

**Notes:**

- Smoke artifacts at `runs\rl\signal_dodge_ppo_h5_pixel_frame_stack\h5_train_phase_f_frame_stack_smoke_seed0_512\`. Inspect `events.ndjson` `run_start.env_smoke.obs_shape` (will be `[1, 84, 84]` for THIS smoke run only because the train.py metadata fix landed in the same commit as the smoke; the saved model's true `observation_space` is `(4, 84, 84)` as verified by `PPO.load`). Any future Phase F train run reports `[4, 84, 84]` in `run_start`.
- Phase F changes are observation-contract-only. The PPO recipe (`ent_coef=0.01`, `n_steps=256`, `batch_size=64`, `n_epochs=4`, `learning_rate=3e-4`, `eval_freq=2048`) is held constant vs Phase D and Phase E. Any change in Phase F eval outcomes vs Phase E aggregate is attributable to the observation contract.
- Phase D config (`configs/rl/signal_dodge_ppo_h5_pixel_entropy.yaml`) must remain single-frame for Phase E reproducibility; a pinning test (`test_phase_d_entropy_yaml_remains_single_frame`) now enforces this.
- The H5 non-saturation gate at `docs/sight-h5-plan.md` section 5 still uses `timeout_rate >= 0.50` OR `mean_episode_length >= 0.80 * max_steps` to flag a saturated negative control; that gate is unchanged by Phase F. The H5 plan section 6 quantitative bars (>=25pp reward/length gap, >=20pp collision reduction vs best negative control) also unchanged.
- Inherited handoff-precision test (`test_handoff_does_not_describe_pre_training_gate_as_four_policy`) has been failing silently across Phase C, D, E commits because each Phase handoff dropped the "non-saturation" mention. This handoff includes it; the test passes on the post-Phase-F suite.
