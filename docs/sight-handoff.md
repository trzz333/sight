# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. FROM-SCRATCH RESULT IS UNRELIABLE (N=3), reliability sweep RUNNING to N=10. K5.8 NoisyNet QR-DQN at 200k clears the bar on 1 of 3 seeds: seed-0 PASS 980.7, seed-1 FAIL 645.9 (two-sided), seed-2 FAIL 741.0 (DEGENERATE, frac_R 0.0). N=3 IQM 789.2, 95% bootstrap CI [645.9, 980.7] straddles bar 930.27, so we cannot yet claim the policy reliably beats OR reliably fails the bar. found-art this session (verified): dependability is a distributional property measured ACROSS SEEDS, not across training steps, and the field's packaged answer is multi-seed + interval estimates (Agarwal et al. 2021, rliable; Henderson et al. 2018). This REDIRECTED the prior queued plan (one 500k seed was the wrong shape, a lucky-seed anecdote). Now running: seeds 3-9 at 200k (train+eval chained, detached) to reach N=10 and tighten the CI. Reliability tooling built (tools\k5_8_reliability_report.py, ADAPT of rliable's IQM + stratified bootstrap CI in numpy/scipy). Reliable above-baseline policies remain BC 1737.3 (imitation) and PPO-finetune 1710.5. K5.7 (epsilon-greedy) closed FAIL. Single-voice governance.

**Last commit:** `2ecec9f` K5.8 N=3 reproducibility SETTLED: from-scratch unreliable (1/3 above bar)

**Current task:** found-art on "dependability in these circumstances" ran and was APPLIED this session. Verdict MIXED -> ADAPT: the reliability-REPORTING half is a solved packaged problem (Agarwal et al. 2021 "Statistical Precipice", rliable library: IQM, stratified bootstrap CIs, performance profiles), verified on PyPI/GitHub this session; the "get a dependable policy" half redirected the queued plan from one-long-seed to multi-seed-at-fixed-budget, which is the field's actual answer. rliable itself will not import on this box (hard-imports arch.bootstrap, no Python 3.14 wheel, needs MSVC toolchain), so I ADAPTED: tools\k5_8_reliability_report.py reimplements IQM + stratified bootstrap percentile CI + performance profile + fraction-above-bar in numpy/scipy (single-task case, faithful to the method, cited). Ran it on the N=3 I have: IQM 789.2, 95% CI [645.9, 980.7], frac_above_bar 0.33, frac_nondegenerate 0.67, perf-profile shows 0/3 seeds reach 1000. With N=3 the IQM degenerates to the mean and the CI spans the full range, which is itself the argument for more seeds. So I LAUNCHED tools\run_k5_8_multiseed_200k.bat (seeds 3-9, train 200k + eval 1000-1009 chained, detached, python PID 15276 confirmed alive on seed-3), each seed writing runs\phase_k\k5_8_noisy_qrdqn_s{S}\seed.sentinel and its eval report json, with a final runs\phase_k\k5_8_multiseed_3_9.sentinel. Reaching N=10 gives a meaningful IQM and a CI that either clears the bar or does not. Env note: rliable removed (broken dep); absl-py, scipy, seaborn installed --user; report tool needs only numpy+scipy.

**Next action:** Monitor the running seeds 3-9 sweep, do NOT launch anything compute-heavy while it holds the CPU (lean-poll <=45s). Check runs\phase_k\k5_8_multiseed_3_9.sentinel (final) and per-seed runs\phase_k\k5_8_noisy_qrdqn_s{S}\seed.sentinel. As seeds finish (each leaves eval_inenv\noisy_eval_inenv_report.json), run python tools\k5_8_reliability_report.py to recompute IQM + 95% bootstrap CI + frac_above_bar across all available seeds; it auto-globs k5_8_noisy_qrdqn*. When N=10 is in, read the report and state the dependability verdict plainly: if the IQM 95% CI lower bound is >= bar 930.27, from-scratch reliably clears the baseline (claimable); if the CI straddles the bar, it is unreliable (frac_above_bar is the honest "clears X/10 seeds" number); if the CI upper bound is < bar, from-scratch at 200k does not clear it. THEN decide the next technical lever with a one-line reason: (a) if frac_above_bar is decent but margin low, a 500k multi-seed sweep tests whether more compute lifts the whole distribution; (b) if frac_above_bar is poor, from-scratch at this budget is done and K5.9 DQfD-lite is the reliable-margin route. The 500k-single-seed plan is RETIRED (found-art: one long seed cannot measure dependability). Surface the N=10 number to Jeff for the portfolio-claim decision (Blockers). Do NOT revisit epsilon-greedy or reward-scaling. Wrap git in cmd /c "...".

**Blockers:** One Jeff-owned DIRECTION question, ripening as N grows: once the N=10 reliability report is in (sweep running), is "from-scratch RL clears the constant-action baseline on X/10 seeds with IQM Y [CI lo, hi], while imitation (BC 1737.3) and PPO-finetune (1710.5) clear it reliably" an acceptable portfolio/writeup headline, or does Jeff want a reliable from-scratch result (CI lower bound above bar) before claiming the mission? Claude's engineering (multiseed sweep now, possibly 500k-multiseed or DQfD-lite next) runs regardless and feeds this; Jeff decides only the framing/claim. No other blocker. Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable.

**Notes:**

- FOUND-ART (dependability): verdict MIXED -> ADAPT. Reporting reliability with few runs is solved and packaged (Agarwal et al. 2021, rliable: IQM, stratified bootstrap CIs, performance profiles; verified PyPI/GitHub). rliable will not import here (arch dep, no py3.14 wheel, needs MSVC), so reimplemented the method in tools\k5_8_reliability_report.py (numpy/scipy, cited). Key redirect: dependability is across-seed, not across-steps, so the old one-500k-seed plan was retired and replaced with a multi-seed sweep (running). Searches run: "deep RL reproducibility seed variance reliable evaluation few runs", "rliable Agarwal IQM stratified bootstrap github". This is the "change the method" move applied to the dependability question itself.
- WHY IT WORKED (vs K5.7): only the exploration MECHANISM changed (NoisyNet replacing epsilon-greedy; identical net_arch, env, reward "none", gamma 0.99). epsilon-greedy QR-DQN went two-sided-but-incompetent (547.7 FAIL); NoisyNet's consistent within-episode perturbation produced competent dodging (980.7 PASS). Confirms the wall was the exploration mechanism, not env/value/reward geometry. The "change the method, do not retry harder" rule paid off. Do not revisit epsilon-greedy or reward-scaling.
- K5.9 DQfD-lite is the remaining lever for a RELIABLE above-bar margin if the multi-seed from-scratch sweep (and any later 500k-multiseed test) stays unreliable. It was the original fallback for pure-RL failure; pure RL cleared the bar on only one lucky seed, so the fallback is back in play but not yet triggered. De-risk notes (docs\k5_9_dqfd_notes.md) and verified asset (runs\phase_k\k5_6_bc\dataset_2000_2035.npz, 64800 two-sided transitions) on disk.
- The detached waiter/sentinel pattern carried the full K5.7->K5.8 chain unattended last session and the K5.8 200k -> eval -> seed-1 repro launches this session. Reliable; standard tool now (tools\run_k5_8_eval.bat, tools\run_k5_8_s1_200k.bat added this session).
- SCOPE/honesty for any writeup: K5.8 is GENUINE from-scratch RL (no demonstrations, NoisyNet exploration); BC is imitation learning. Do not conflate the two. The from-scratch claim is N=3 training seeds (1 PASS / 2 FAIL on held-out eval seeds 1000-1009); state this honestly, it is unreliable not robust. reward_shaping "none" stays load-bearing (best constant 845.7 < bar 930.27). runs/ is gitignored, so eval report jsons live only on StrongerJr.
