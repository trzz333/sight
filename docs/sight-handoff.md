# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Demo-0 visible-AI artifact complete. K0 closed, K1 architecture probe still held pending Jeff's explicit go signal. Phase nomenclature unchanged (H5 amended); Demo-0 is a parallel visibility slice that interrupted the K-series ladder per GPT's correction (diagnostics do not substitute for a watchable artifact).

**Last commit:** `726b607` Demo-0: visible AI playing Signal Dodge end-to-end artifact.

**Current task:** Demo-0 visible-AI artifact landed. New tool `tools\demo0_visible_play.py` loads a trained CnnPolicy PPO model, launches Signal Dodge in windowed pixel mode via `GodotSignalDodgeEnv(headless=False)`, steps with `model.predict(obs, deterministic=True)`, and writes upscaled frame PNGs, an MP4 (mp4v codec, OpenCV `VideoWriter`), per-step NDJSON, and a manifest. Demo-0 was run against Phase E seed=2 entropy-recipe checkpoint (`runs\rl\signal_dodge_ppo_h5_pixel_entropy\h5_train_phase_e_seed2_entropy_10k\model.zip`), eval seed=1008, 1 episode, fps=20, upscale=4. Result: 1800 steps, total_reward=1800.0, terminated=false, truncated=true, terminal_reason="timeout", wall=50.0 s. Behavior: 100% `left` across all 1800 steps. Survival is coincidence of seed 1008 hazard placement, not avoidance competence. This confirms the K0-10k wedge / constant-action diagnosis at deployment time. Evidence at `docs\demo0-visible-ai-playing-evidence.md` with artifact sha256s. Artifact sha256: mp4 `3a61c098…`, steps.ndjson `33b7a432…`, manifest `0e341cca…` (full hashes in evidence doc; all artifacts gitignored under `runs/demo0/`).

**Next action:** Hold for Jeff's go signal. K1 architecture probe (`policy_kwargs.net_arch = dict(pi=[64], vf=[64])`, train_seed=2, 10000 timesteps, same entropy YAML otherwise) remains the structurally correct next mechanism slice once Jeff signals. Per the prior GPT planning turn, K1 should also be preceded by a small patch to `tools\h5_training_entropy_probe.py` so `per_update_digest` surfaces pre/post deterministic argmax action, pre/post deterministic argmax fraction, and post mean action probabilities (the probe records these in `policy_state` already; just hoist them into the digest). K1 should compare primarily against K0-10k's deterministic trajectory and secondarily against Phase E seed=2's basin. K2 train-seed asymmetry remains held behind K1.

**Blockers:** None.

**Notes:**

- Demo-0 success criterion met: a human opens `runs\demo0\trained_seed2_eval1008\demo0.mp4` and watches Signal Dodge controlled by the trained CNN policy. The viewing experience is honest: the paddle pins to the left and rides the seed. The artifact does not certify learning closure; the wedge basin is exactly what K0-10k diagnosed.
- The existing `sight_agent.rl.evaluate` / `h5_baseline_cli` eval pipelines write NDJSON metrics only, with no frame or video capture, so a small dedicated `tools\demo0_visible_play.py` was the right scope rather than extending an existing CLI. Reuses `GodotSignalDodgeEnv` directly and `resolve_godot_kwargs` for env construction. ~280 lines, no new infrastructure.
- Visible pixel-mode Godot at game speed runs ~36 steps/s wall on this hardware (1800 steps in 50.0 s, ~6x slower than headless training). Single-episode 1800-step budget plus startup fits inside a 60-90 s wall window, which is just barely above MCP's 4-minute timeout for unbuffered Python with no incremental output. The first invocation under `interact_with_process` returned no result inside the MCP timeout window even though the python process itself completed and wrote all artifacts cleanly; verified post-hoc via filesystem inspection. For future visible-mode demos with longer or multi-episode budgets, use the bat-with-sentinel wrapper pattern (per K0-10k learnings) rather than inline `interact_with_process`.
- `SIGHT_GODOT_EXE` set inline in the active cmd shell before invocation is required; user-scope env vars are not reliably inherited by Godot launches under this project's transport pipeline. Also: `set FOO=bar && echo %FOO%` does not work in cmd.exe because `%FOO%` is expanded at command-line parse time before `set` executes. Set on its own line, then read on a subsequent line.
- Phase H basin re-derivation is **not** required before K1 (resolved by GPT in the prior turn). Phase H compared trained-model probability distributions on fixed tapes and recorded model argmax directly; the sampled-vs-deterministic conflation was in the K0-2048 narrative, not in the Phase H comparator result. Demo-0 does not change this. The deterministic argmax K0-10k recorded (`policy_state.top_argmax_action`) is the same statistic Demo-0 sees at deployment: locked `left` for this train run.
