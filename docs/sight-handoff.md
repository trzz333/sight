# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** VZD-3 deadly_corridor. Stage 2 (skill 3 + shaping + reward normalization) COMPLETE and PASSED. Next gate is the skill-5 resume-finetune.

**Last commit:** 0f1eb6f vzd: VZD-3 stage 2 PASSES; corridor solved at skill 3

**Current task:** Stage 2 passed decisively. `runs\vzd\ppo_deadly_corridor_s3_shaped\summary.json`: **mean 2279.14 / IQM 2279.43** over 30 deterministic raw episodes at skill 3, 1,501,472 steps, 8,445.7s, 118.4 steps/s. Bar was IQM decisively above the flat skill-3 IQM 683.9 (same skill, same raw eval): cleared by 3.3x, and clears the ~767 untrained anchor by 3x too, so that anchor's known methodology defect does not touch the verdict (HIGH). The distribution is the real result: **30/30 episodes reach the armor** (2276.2-2281.6) against the flat run's **4/30**, with 15/30 stuck on the untrained modal 664.1885. The two fixes moved the dominant mode, which stage 1 failed to do. Optimizer healthy at end and clearly distinct from the section-3a collapse: entropy_loss -2.08 to -0.08 but approx_kl 0.0028 and clip_fraction 0.0317 stay NONZERO while ep_rew_mean climbs -1160 to 3120 and explained_variance hits 0.972. Flat froze at exactly 0.0 kl with reward flat; this sharpened onto a working solution. Three UNKNOWNs stand and are recorded in `docs\vzd-deadly-corridor-findings.md` section 4a: (1) **fight or run past** is unresolved, raw reward is distance plus death penalty so 2279.5 is consistent with sprinting to the vest without killing anything, and section-2 prior art explicitly warns skill-3 success can be combat-free, which makes this the load-bearing risk for skill 5; (2) **single seed**, n=1, deterministic eval on a deterministic map measures which mode one policy landed in, not a distribution, so seeds are needed, not more episodes; (3) **entropy -0.08 leaves little headroom** to adapt at skill 5.

**Next action:** Answer "fight or run past" before spending 4h on skill 5, because it decides whether the skill-5 step is a finetune or a redesign. Record KILLCOUNT/HITCOUNT during a 30-episode deterministic eval of `runs\vzd\ppo_deadly_corridor_s3_shaped\model.zip` at skill 3 via `unwrapped.game.get_game_variable` (HITCOUNT/KILLCOUNT verified readable regardless of cfg), and record the gameplay clip from the same pass for the demo reel. If it kills the first pair, launch the skill-5 resume-finetune from `model.zip` under Task Scheduler with `--ent-coef` raised (entropy -0.08 has no exploration left to re-adapt). If it runs past, skill-5 transfer will likely fail as section 2 predicts, so go to intrinsic exploration (RND/ICM, already scouted in section 2) rather than retrying the curriculum harder.

**Blockers:** None requiring Jeff.

**Notes:**

- **Long jobs are Task Scheduler tasks, never detached children.** `runs\vzd\_run_s3_shaped_task.cmd` and `runs\_run_monitor_task.cmd`; `schtasks /Run /TN Sight-Monitor` to restore the monitor (http://127.0.0.1:8791/monitor.html, verified 200). Detached children died 3x for training and 3x for the monitor; the OS-owned run then completed 1.0M steps in one leg, rc=0, zero restarts. `/RL HIGHEST` and `/SC ONLOGON` both need elevation and were dropped. Deleting a task kills its running instance.
- **The 3db4777 "ViZDoom engine death" diagnosis was WRONG (findings section 8).** Zero `[SafeDoom]` rebuilds in 1.5M steps, no worker-side traceback, and the supervisor plus the unrelated monitor died too. Root cause of the mass kill is still **UNKNOWN** (LOW on every candidate; `nhi` Thunderbolt events bracket it). Task Scheduler as the fix is **MEDIUM, not HIGH**: n=1. If it recurs, `supervisor.log` plus `SUP_HEARTBEAT` now distinguish killed from crashed, which run 3 could not.
- **Method lesson, cost 3 runs:** the SafeDoom fault-injection test exercised the path I built (`game.close()` raises a catchable exception), not the path that was failing (worker vanishes, no exception). It produced confidence, not evidence. Verify against the observed failure signature, not against the fix.
- Resume is real and exercised: run 4 restored VecNormalize stats from the step-matched 500k `.pkl` and ran to 1.5M. `--ckpt-every` 50k, `save_vecnormalize=True`. Without it a restart re-estimates the return std from 1.0 and re-inflates the returns that caused the collapse, undoing the fix it protects. `--resume` makes `--steps` ADDITIONAL; the supervisor owns the total via `--target`.
- vizdoom 1.3.0, sb3 2.8.0, Python 3.14 (verified). Eval is deliberately RAW so numbers compare across runs; shaped ep_rew_mean is NOT comparable to the flat curve. Skill 1 is useless as an eval point (untrained evals ~2280 there). SafeDoom must sit INSIDE ShapedCorridorReward so `unwrapped.game` resolves to a rebuilt engine; re-run `runs\vzd\_test_safedoom_fault.py` after any wrapper-order change. `runs\` is gitignored.
