# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. FROM-SCRATCH RESULT IS UNRELIABLE (N=3 settled). K5.8 NoisyNet QR-DQN at 200k clears the bar on 1 of 3 seeds: seed-0 PASS 980.7 (two-sided), seed-1 FAIL 645.9 (two-sided non-degenerate), seed-2 FAIL 741.0 (DEGENERATE, never chose right, frac_R 0.0). Mean-of-means 789.2 < bar 930.27; best constant 845.7 beats two of the three trained seeds. The single-seed PASS was variance, now confirmed twice. From-scratch RL is achievable on a lucky seed but NOT reliable at this compute budget. Two 200k seeds failed (one degenerately), so "change the method, do not retry 200k harder" is now in force. Reliable above-baseline policies remain BC 1737.3 (imitation) and PPO-finetune 1710.5. K5.7 (QR-DQN epsilon-greedy) closed FAIL. Single-voice governance.

**Last commit:** `2ecec9f` K5.8 N=3 reproducibility SETTLED: from-scratch unreliable (1/3 above bar)

**Current task:** N=3 reproducibility sweep of K5.8 NoisyNet from-scratch is COMPLETE and the result is SETTLED as unreliable. All three 200k seeds trained clean (each DONE 0, model written) and were evaluated in-env on held-out seeds 1000-1009; verdicts anchored to runs\phase_k\<run>\eval_inenv\noisy_eval_inenv_report.json, re-read this session: seed-0 PASS mean 980.7 frac_L/S/R 0.055/0.862/0.082 two-sided; seed-1 FAIL mean 645.9 frac 0.155/0.608/0.237 two-sided non-degenerate; seed-2 FAIL mean 741.0 frac 0.586/0.414/0.0 DEGENERATE (collapsed to left-or-stay, chose right zero times across all 10 seeds). Mean-of-means 789.2, below bar 930.27; only 1/3 above bar; best_constant 845.7 beats seed-1 and seed-2. The two failures fail differently (one low-but-two-sided, one one-directional), which is the signature of high training variance, not a single fixable bug. Conclusion: from-scratch NoisyNet QR-DQN at 200k clears the baseline on a minority of seeds and is NOT a reliable result. The earlier "milestone met" claim was single-seed variance and is retracted. DECISION TAKEN (Claude, technical): option (b) reframe honestly as "from-scratch above-baseline is achievable but unreliable at 200k (1/3 seeds)", plus ONE narrow time-boxed test of option (a), a single longer-horizon run (500k, seed 0) to check whether the instability is just undertraining, before deciding whether longer training is worth a full multi-seed sweep. Not reviving K5.9 DQfD-lite (c) yet: that adds scope to chase a margin before the cheap "does it just need more steps" question is answered. Reliable above-baseline policies remain BC 1737.3 and PPO-finetune 1710.5.

**Next action:** Launch ONE longer-horizon from-scratch run to test the undertraining hypothesis: create tools\run_k5_8_s0_500k.bat as a copy of tools\run_k5_8_s2_200k.bat with --timesteps 500000 --seed 0 --out runs\phase_k\k5_8_noisy_qrdqn_s0_500k and sentinel ..._s0_500k.sentinel; launch detached via start "" /b cmd /c. While it runs (~7-8hr at ~17 fps, lean-poll <=45s, do NOT launch anything else while it holds the CPU), do nothing else compute-heavy. On sentinel DONE, eval it: set SIGHT_GODOT_EXE inline, python tools\k5_8_noisy_eval_inenv.py --run runs\phase_k\k5_8_noisy_qrdqn_s0_500k --seeds 1000-1009. If 500k PASSes clean and non-degenerate, the instability was undertraining: queue a 500k multi-seed (1,2) sweep to confirm reliability at the higher budget. If 500k FAILs or is degenerate, longer training does not fix it: stop spending compute on from-scratch, lock the honest framing (achievable-but-unreliable, 1/3 at 200k), and the only remaining lever for a RELIABLE margin is K5.9 DQfD-lite (de-risk notes on disk). Do NOT revisit epsilon-greedy QR-DQN or reward-scaling. Do NOT retry 200k seeds. Wrap git in cmd /c "...".

**Blockers:** One Jeff-owned DIRECTION question, now ripe (reliability is characterized, so it is no longer premature): is "from-scratch RL clears the constant-action baseline on a minority of seeds (1/3 at 200k), while imitation (BC 1737.3) and PPO-finetune (1710.5) clear it reliably" an acceptable headline for the portfolio/writeup, or does Jeff want a reliable from-scratch result before claiming the mission? Claude's technical next step (the 500k undertraining test) runs regardless and informs but does not decide this. Jeff decides only the framing/claim, not the engineering. No other blocker. Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable.

**Notes:**

- N=3 SETTLED + DIRECTION: from-scratch NoisyNet QR-DQN at 200k is 1/3 above bar (seed-0 980.7 PASS, seed-1 645.9 FAIL two-sided, seed-2 741.0 FAIL degenerate). Mean-of-means 789.2 < 930.27. The "milestone met" claim is retracted as single-seed variance. Honest standing: above-baseline from-scratch is achievable but unreliable at this budget. Next technical lever is longer training (500k test queued); next Jeff-owned lever is whether the unreliable-but-real result is an acceptable portfolio claim. No locked from-scratch PASS claim until a reliable result exists.
- WHY IT WORKED (vs K5.7): only the exploration MECHANISM changed (NoisyNet replacing epsilon-greedy; identical net_arch, env, reward "none", gamma 0.99). epsilon-greedy QR-DQN went two-sided-but-incompetent (547.7 FAIL); NoisyNet's consistent within-episode perturbation produced competent dodging (980.7 PASS). Confirms the wall was the exploration mechanism, not env/value/reward geometry. The "change the method, do not retry harder" rule paid off. Do not revisit epsilon-greedy or reward-scaling.
- K5.9 DQfD-lite is the remaining lever for a RELIABLE above-bar margin if longer-horizon from-scratch (the 500k test) also proves unreliable. It was the original fallback for pure-RL failure; pure RL succeeded only on one lucky seed, so the fallback is back in play but not yet triggered. De-risk notes (docs\k5_9_dqfd_notes.md) and verified asset (runs\phase_k\k5_6_bc\dataset_2000_2035.npz, 64800 two-sided transitions) on disk.
- The detached waiter/sentinel pattern carried the full K5.7->K5.8 chain unattended last session and the K5.8 200k -> eval -> seed-1 repro launches this session. Reliable; standard tool now (tools\run_k5_8_eval.bat, tools\run_k5_8_s1_200k.bat added this session).
- SCOPE/honesty for any writeup: K5.8 is GENUINE from-scratch RL (no demonstrations, NoisyNet exploration); BC is imitation learning. Do not conflate the two. The from-scratch claim is N=3 training seeds (1 PASS / 2 FAIL on held-out eval seeds 1000-1009); state this honestly, it is unreliable not robust. reward_shaping "none" stays load-bearing (best constant 845.7 < bar 930.27). runs/ is gitignored, so eval report jsons live only on StrongerJr.
