# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K (K3.5 + K3.5b complete: fixed reward-magnitude scaler at divisor 30 PASSES the K3.5 contract at both 2048-ts and 10k. `latent_vf_live_post = 128/128` across all 40 updates of the 10k confirmation. Mechanism finding: absolute reward magnitude is load-bearing under Adam, not relative drift ratio. The original Phase K eval anomaly is NOT resolved by K3.5; the deterministic-argmax panel wedge persists from update 1 onward, mechanistically separate from value collapse. K1-extended remains parked).

**Last commit:** `a4239af` Phase K K3.5b 10k confirmation passes contract gates; deterministic-argmax panel wedge is mechanistically separable and persists across all K3.x interventions.

**Current task:** K3.5b ran a 10k-ts confirmation on `/30` per the final K3.5 contract decision rule. Same params as K3.5: seed 3, pi=[64], vf=[128], `vf_coef=0.5`, `--value-bias-init none`, `--reward-scale-divisor 30`. 40 PPO updates, K-C verdict, elapsed 313.5 s. Sustained-life gates: `latent_vf_live_post` min = 128 (PASS); `fixed_panel_value_predictions_std_post` min = 2.23e-3 at u6 (PASS, >> 1e-6); rollout `top_action_fraction` max = 0.629 at u6 (PASS, < 0.95 wedge threshold); `post_ratio` max = 25.143 at u35 (single transient spike, 32 of 40 updates < 1.0, no co-occurring collapse signature). Reward drift across the full probe: raw `ret_mean` first-to-last 14.118 -> 75.639 (5.36x), saturates near 90-step episodes by u30 and mean-reverts to ~75 by u40. K3.5 PASS classification holds at 10k. Launched via bat-sentinel pattern (`%TEMP%\run_k3_5_confirm.bat`, `PYTHONUNBUFFERED=1`) because 5+ min wall time exceeds MCP shell timeout ceiling. Evidence in `docs/k3-5b-reward-scaling-10k-confirmation-evidence.md`. CSV at `runs/phase_k/k3_5_confirm_reward_scale_div30_10000_table.csv`. Analysis script `tools/k3_5b_extract.py`.

**Next action:** GPT scoping needed on K-series direction. K3.5 closes the value-shock collapse track. Two independent paths emerge, scope-distinct:

1. Pursue the deterministic-argmax panel wedge as the active K-series target (call it K4). The wedge is present at update 1 of a freshly-initialized CnnPolicy (`panel_top_argmax_fraction = 1.000`, `panel_num_det_actions = 1` on EVERY update from u1 onward), mechanistically separate from value collapse, and the most plausible upstream explanation for the original Phase K bit-identical eval anomaly. Candidate K4 mechanisms: action-head initialization, CnnPolicy default weight scaling, log-softmax tie-breaking at near-uniform initialization, or features-extractor output uniformity producing identical logits across panel obs. Diagnostic slice would inspect the raw action-net logits on the panel at update 0 before any training step.
2. Step back to the original Phase K eval anomaly directly. Run the K3.5 recipe (`--reward-scale-divisor 30`, `--value-bias-init none`, otherwise default H5 entropy config) at full training budget (10k or 25k), checkpoint at multiple stages, run the standard H5 eval pipeline against those checkpoints. If eval results differ across checkpoints, K3.5 has resolved the eval anomaly downstream of the value head. If they remain bit-identical, the deterministic-argmax wedge is confirmed as the operative eval-time mechanism and K4 becomes the focused intervention.

Path 2 is the cheaper falsification: it answers a single direct question (does K3.5's value-head rescue translate to eval-time behavior change?) with the existing H5 eval pipeline. Path 1 is the deeper mechanism investigation. Recommend Path 2 first, then conditional on its result either close out the original anomaly (eval differs) or open K4 (eval still bit-identical).

**Blockers:** None requiring Jeff.

**Notes:**

- The deterministic-argmax panel wedge is PRESENT AT UPDATE 1 of the K3.5b 10k run, with a freshly-initialized CnnPolicy and a reward-scaled rollout. It is NOT a training-collapse artifact under any K3.x intervention. K3.4 evidence already noted this: "constant_action_attractor is True for every update (matches K3.3). The bias-init does not affect the deterministic argmax wedge on the fixed panel." K3.5 also does not affect it. The wedge survives every K-series mechanism intervention tested so far.
- Rollout-time stochastic behavior is healthy under K3.5b. Rollout `top_action_fraction` max 0.629, top action shifts between left and right across rollouts (8 left-dominant, 22 right-dominant, 0 stay-dominant of 40 updates), sampling keeps all three actions in play. The wedge is a deterministic-argmax property, not a sampling-distribution property.
- K3.5 reward drift behavior at 10k saturates near 90-step survival episodes around u30 and mean-reverts to ~75 by u40. K3.4's secondary-mechanism concern ("value head chases an ever-growing target") is bounded under K3.5 scaling: per-update raw `ret_mean` deltas stay in the 1-15 range (scaled 0.03-0.5), small enough for Adam to absorb each catch-up.
- The K3.5b patch added only `tools/k3_5b_extract.py` as new code; no source files in `src/` or `games/` or `tools/h5_training_entropy_probe.py` were modified. The 10k confirmation reused the K3.5 patched probe directly. Bat-sentinel pattern (`%TEMP%\run_k3_5_confirm.bat`) is local-only and not committed.
- Evidence and CSV layout: `docs/k3-5-reward-scaling-evidence.md` (K3.5 2048-ts), `docs/k3-5b-reward-scaling-10k-confirmation-evidence.md` (K3.5b 10k confirm), `runs/phase_k/k3_5_smoke_reward_scale_div30_512.ndjson` (smoke), `runs/phase_k/k3_5_reward_scale_div30_2048.ndjson` (K3.5 /30), `runs/phase_k/k3_5_reward_scale_div100_2048.ndjson` (K3.5 /100), `runs/phase_k/k3_5_confirm_reward_scale_div30_10000.ndjson` (K3.5b 10k confirm), `runs/phase_k/k3_5_confirm_reward_scale_div30_10000_table.csv` (gitignored). Analysis scripts `tools/k3_5_extract.py` and `tools/k3_5b_extract.py`.
