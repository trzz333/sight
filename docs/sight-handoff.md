# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** H5 amended; Phase H docs-and-tools-only logit-distribution comparator complete on Class A (Phase E seed1 vs Phase G seed1, stay tape, eval seed 1000), Class B (Phase E seed2 vs Phase G seed2, left tape, eval seed 1000), and the optional G1 vs G2 cross-basin runs on both tapes. The four-model SHA-256 fingerprints are all distinct, the four policy state-dict blake2b hashes are all distinct, and observation hashes are 92.8% to 99.86% unique across the two tapes; model loading and observation freshness are both falsified as the proximate H5 pathology. Within-basin (E1 vs G1) probability distributions are essentially identical (prob_l1 mean 0.0029, sym_kl mean 0.0057) with both models above 99.85% mass on `stay`. Within-basin (E2 vs G2) probability distributions differ substantially (prob_l1 mean 0.724, sym_kl mean 0.884) but the argmax is `left` on every step in both reward variants. Cross-basin (G1 vs G2 on either tape) same-argmax fraction is **0.0000**: G1 picks `stay` on 100% of steps with mean margin 1.0000 regardless of which trajectory it sees; G2 picks `left` on 100% of steps with mean margin 0.97 regardless. prob_l1 mean cross-basin is 1.996 on the stay tape and 1.969 on the left tape, approaching the theoretical maximum of 2.0. The trained policies are observation-insensitive at the policy-output layer. The proximate H5 pathology is relocated upstream of the action head into the feature extractor and/or policy MLP.

**Last commit:** `0ca198e` docs(h5): phase H logit comparator evidence + tool.

**Current task:** Phase H evidence at `docs/h5-phase-h-logit-comparator-evidence.md` covers tool, methodology, model-loading verification, observation freshness, Class A, Class B, cross-basin smoking gun, branch-table mapping per the Phase H scope note, recommended next slice, and a reproduction recipe. Comparator at `tools/h5_logit_compare.py` is docs-and-tools-only: no training, no env code change, no config change. Comparator outputs at `runs/phase_h/logit_compare_{classA_stay,classB_left,g1_vs_g2_stay,g1_vs_g2_left}.ndjson` plus sibling `.summary.json` and per-run Godot eval sidecars (all gitignored). Driver log at `runs/phase_h/driver.log` with the per-run argmax/entropy/margin/pair-distance lines.

**Next action:** GPT plans the next slice. Recommended by Claude: an **activation comparator** across `model.policy.features_extractor` and `model.policy.mlp_extractor.policy_net` outputs on fixed observation sequences (the same stay-tape and left-tape sequences used in Phase H), per-step variance and PCA on each layer's outputs, and within-model branch logic on whether the encoder has collapsed or the policy MLP is saturating. Stochastic-action eval ablation is the wrong next lever despite being on the Phase H branch table: Class A both networks above 99.8% on `stay` means stochastic eval changes nothing, and the cross-basin runs show even Class B's argmax is observation-insensitive, so stochastic eval would only re-derive the Class B entropy gradient (E2 left 0.62, G2 left 0.98) without addressing the constant-action pathology. The activation comparator is Grok-trigger eligible per charter: RL internals on the encoder/policy-head boundary and a phase-gate-level finding about whether the current H5 pixel-CNN baseline can ever learn observation-conditioned behavior. Jeff approval required before execution.

**Blockers:** none.

**Notes:**

- Cross-basin smoking gun. G1 receiving the left-tape observation sequence (a 1383-step trajectory G1 has never produced under its own training) still picks `stay` 100% with mean margin 1.0000 and mean entropy 0.0002. G2 receiving the stay-tape observation sequence still picks `left` 100% with mean margin 0.9963. The two tapes share no obs hashes (player x-position differs by hundreds of pixels for the bulk of frames) and yet each network's action distribution is bit-shape identical across the two trajectories.
- Model fingerprints. Phase E seed1 sha256=2126014dae0cedbf, Phase E seed2 sha256=5e5fbba2d7580bfb, Phase G seed1 sha256=51e02ecdb6234b64, Phase G seed2 sha256=705f56596a69a415. All four state_dict blake2b-128 hashes distinct. Param count uniform at 5,036,004 across all four (NatureCNN + Discrete(3)).
- Class B reward shaping. Phase G's shaped reward did produce a measurable distributional effect in Class B (E2 left 0.62 → G2 left 0.98; entropy 0.888 → 0.090; margin 0.350 → 0.968). The shaping concentrated probability mass on the already-selected action; it did not move any model across basins. Class A showed no probability-space effect at all (prob_l1 mean 0.0029).
- SB3 logit semantics. `policy.get_distribution(obs).distribution.logits` returns log_softmax, not raw action-head logits. The Phase H comparator's pairwise metrics (centered_logit_l2, prob_l1, sym_kl) are well-defined on the normalized form. Raw action-head logit extraction would couple to SB3-internal `mlp_extractor.policy_net` and `action_net` layout and is deferred to the activation-comparator slice if needed.
- DC operational note. `start "" /B cmd /c <bat>` invoked from inside an `interact_with_process`-spawned cmd shell reproducibly crashes the spawning shell with "Input redirection is not supported"; the .bat itself still runs to completion. Reliable detach pattern is `start "title" /MIN cmd /c <bat>` (separate minimized window). `runs/phase_h/driver.done` is the source of truth for driver completion; poll it rather than relying on the spawning shell to survive. Total Phase H driver wall time on StrongerJr was about 2 minutes 17 seconds for four sequential windowed-Godot pixel-mode runs.
