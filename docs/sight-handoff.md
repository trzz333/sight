# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** H5 amended; first 3-seed 10k shaped-reward training run executed and evaluated; amendment hypothesis falsified at the eval-trajectory level. Phase G shaped models (`alpha=0.05`, `lookahead_band=270`, `safe_lateral_distance=180`) produced eval trajectories byte-identical to Phase E across all 30 paired (train_seed, eval_seed) cells. Six trained networks (Phase E + Phase G across train seeds 1, 2, 3) on two distinct reward functions reduce to two eval-trajectory equivalence classes keyed by train_seed only: Class A (train_seed=1, `action_wire=1` stay every step, player frozen at x=360, collision 1.0); Class B (train_seed=2 and 3, identical to each other, `action_wire=0` left every step, wedge at x=16 92% of frames, collision 0.9). `action_wire=2` (right) selected by zero networks across 22 974 eval step rows. Deterministic-argmax pathology from memory ("Three distinct trained networks with different weights produce identical per-seed eval outcomes") is strongly confirmed and is the proximate H5 bottleneck. Implementation slice `b41bffc` is not at fault: smoke evidence proved the shaped-reward bonus surface is non-degenerate under non-trivial action sampling, default-path schema regression remains byte-equal, and shaped vs default trajectory parity holds end-to-end. The shaping is falsified as a sufficient solo lever, not as a tool.

**Last commit:** `716ed73` docs(h5): phase G shaped-reward 3-seed 10k evidence + shaped config.

**Current task:** Phase G evidence at `docs/h5-phase-g-shaped-evidence.md` with per-train-seed and per-eval-seed paired tables, action and position distributions, the falsification analysis at the eval-trajectory level, the monitoring requirement discharge, the implementation-not-at-fault statement, the recommended next lever for GPT planning, and the reproduction recipe. Shaped config at `configs/rl/signal_dodge_ppo_h5_pixel_entropy_shaped.yaml` with the four reward-shaping keys and a distinct `run.name` so shaped artifacts do not co-mingle with Phase E artifacts. Phase G train and eval artifacts live under `runs/rl/signal_dodge_ppo_h5_pixel_entropy_shaped/` (gitignored). Driver at `runs/phase_g/run_phase_g.bat` (gitignored). No code change to env, factories, configs, godot, or tests; the implementation slice and its 32 amendment tests plus 420 default-tier remain authoritative.

**Next action:** GPT plans the diagnostic slice. Per amendment section 7 the right next lever is the game-state-dynamics sanity check, not another reward variant and not entropy tuning. Recommended concrete first cut (Claude opinion): a docs-and-tools-only logit-distribution comparator that loads Phase E seed=1 model and Phase G seed=1 model, runs them step-by-step against the same eval trajectory at seed 1000, dumps per-step action logits from both, and compares. If logits differ meaningfully but argmax does not, the issue is the deterministic decision rule (next slice: an eval-side stochastic-action ablation). If logits themselves are nearly identical across radically different reward gradients, the issue is upstream of the action head (encoder, observation freshness, frame-stack contract, observation channel sanity). Memory flags this class as Grok-trigger-worthy. No training and no env change in the comparator slice. Jeff approval required before execution.

**Blockers:** none.

**Notes:**

- Phase G eval reduces to two equivalence classes across six trained networks. Class A: train_seed=1, mean length 606, collision 1.0, action_wire=1 (stay) on every step, x stays at 360. Class B: train_seed=2 and 3 (identical), mean length 845.7, collision 0.9, action_wire=0 (left) on every step, x=16 for 92% of frames. Class B includes one truncation (eval_seed=1008, length 1800) and nine collisions. Phase E showed exactly the same two classes with the same per-eval-seed lengths. Reward function had zero effect at the eval-trajectory level.
- Monitoring requirement discharged. Trained-policy mean `frac_active_threat_saturated` over 30 rollouts is 0.3955, below the 0.50 bar attached in `docs/h5-reward-amendment-smoke-evidence.md` section 4. `safe_lateral_distance` does NOT require pre-emptive revision; the constants are not the proximate issue. Per-train-seed aggregates: train_seed=1 mean 0.228, train_seed=2 mean 0.479, train_seed=3 mean 0.479. Class B is closer to the bar than Class A because the wedge-at-x=16 policy parks where most lookahead-band hazards are at hazard_x > 196 and lateral_clearance saturates to 1.0.
- Localization. The seeded-random rollouts in the smoke produced observation-correlated trajectories with varied per-seed lengths and meaningful shaped-reward variance under the SAME env, protocol, NDJSON logging, and shaped-reward computation. Env / protocol / logging / shaping all work. The pathology is localized to the trained `CnnPolicy.predict(deterministic=True)` call selecting a single observation-independent action per train_seed.
- Wall-time baseline. Phase G per-seed wall time on StrongerJr was 6 min train + 1 min eval, 21 min end-to-end for the 3-seed run. Phase E baseline was 362 s per seed train. The shaped reward did not measurably change wall time. Reproduction recipe in `docs/h5-phase-g-shaped-evidence.md` section 10.
- Operational reminders that survived this session. `SIGHT_GODOT_EXE` must be set inline before any Godot-touching python invocation. Pixel mode requires windowed Godot. Sentinel-and-poll pattern is mandatory for runs that exceed the 4-minute DC tool-call ceiling; multi-minute pings inside `interact_with_process` get their response dropped silently while the underlying process keeps running. Use shorter polls (~30 s) and read the sentinel and log markers separately when batches are long. `runs/` remains gitignored; durable evidence lives in `configs/` and `docs/`.
