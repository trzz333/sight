# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** VZD-3 deadly_corridor. Skill-3 stage 2 PASSED and combat-verified. Skill-5 resume-finetune COMPLETE and PASSED on reward; combat-verification pending.

**Last commit:** fb7ff99 vzd: resolve fight-or-run-past (FIGHT), launch skill-5 finetune (skill-5 run finished after this commit; results live in runs, not yet written into findings).

**Current task:** Skill-5 resume-finetune is DONE and PASSED the reward bar. `runs\vzd\ppo_deadly_corridor_s5_ft\summary.json`: raw skill-5 IQM 2279.67, mean 2199.67 over 30 deterministic episodes, 1 death (-115.9), other 29 at ~2280. Supervisor confirms clean finish (`leg 0 exited rc=0 after 13922s`, `trainer exited clean`, DONE sentinel). This clears the pre-registered bar decisively: flat skill-5 baseline collapsed at IQM 93.6, and this policy's own cold start at skill 5 was IQM ~93.3, so ~93 -> ~2280 is real transfer. The `--ent-coef 0.05` override is vindicated (entropy re-climbed from -0.08, giving the near-deterministic policy room to re-adapt; without it it could not). IQM recomputed from the raw rewards array matches stored to the penny (audit anchor this session). CAVEAT held open: skill-5 IQM 2279.67 is nearly identical to skill-3 IQM 2279.43. The corridor length and armor position are identical across skills (only enemy speed/accuracy differ), so raw distance-reward alone cannot distinguish "fights well at skill 5" from "still just reaches the vest, faster enemies simply failed to stop it." The combat probe resolves this. Single seed.

**Next action:** Run the combat probe at skill 5 to resolve the caveat above: `.venv-c1\Scripts\python.exe tools\vzd_probe_combat.py --model runs\vzd\ppo_deadly_corridor_s5_ft\model.zip --env-id VizdoomDeadlyCorridor-v1 --doom-skill 5 --episodes 30 --out runs\vzd\ppo_deadly_corridor_s5_ft\combat_probe.json --record runs\vzd\demos\corridor_s5_ft.mp4`. Read kills_mean / episodes_with_a_kill / deaths (KILLCOUNT/HITCOUNT/DAMAGE_TAKEN are the trustworthy fields; SHOTS_FIRED/accuracy are known-broken). If it fights (kills > ~2/ep, low deaths): VZD-3 is DONE. Then write the results row into `docs\vzd-deadly-corridor-findings.md` section 5 (skill-3 IQM 2279.43 combat-verified 5k/5h; skill-5 IQM 2279.67 + its combat numbers), commit the findings update, and cut the public demo reel from the two clips. If it runs past (kills ~0): the reward passed but the behavior did not transfer; the score is a distance artifact, and the honest write-up says so. Escalate per findings section 6 order or go to intrinsic exploration (RND/ICM). Either way, seeds are owed before any decisive transfer claim goes in the README.

**Blockers:** None requiring Jeff.

**Notes:**

- **MONITOR: the detached-child pattern is dead, do not use it.** It died 7 times (6 prior + again this session): the Task Scheduler `Sight-Monitor` task launches a Python script that spawns a DETACHED child and exits, so Task Scheduler marks itself "Ready" (stopped) while the orphaned child dies unseen, and HTTP 200 at one instant proves nothing. Jeff could not see the monitor for exactly this reason. Working pattern now: run the server as a FOREGROUND long-lived process bound to `0.0.0.0` (not 127.0.0.1, which is unreachable from Jeff's other devices) via `python -c "from functools import partial; from http.server import ThreadingHTTPServer, SimpleHTTPRequestHandler; H=partial(SimpleHTTPRequestHandler, directory=r'C:\Projects\Sight\runs\vzd'); type('S',(ThreadingHTTPServer,),{'daemon_threads':True})(('0.0.0.0',8791),H).serve_forever()"`. LAN URLs for Jeff: **http://192.168.68.108:8791/monitor_s5.html** (Wi-Fi) or **http://192.168.1.178:8791** (Ethernet). Verify by fetching the log the page polls and confirming latest `total_timesteps` is current, NEVER by status 200 alone. `monitor*.html` live under gitignored `runs\` (not version-controlled; each is hardcoded to one run's LOG/SUM/TARGET, recreate by copy+rewire). Rebuilding this as a robust always-on service (0.0.0.0, survives sessions, self-restarts, correct run auto-selected) is a real open item.
- **Long training jobs: Task Scheduler tasks worked (`Sight-VZD3-S5` ran the full 3.9h clean, rc=0).** The detached-child ban applies to the monitor; training under Task Scheduler is fine. Root cause of the runs-2/3 mass kill is still UNKNOWN; Task Scheduler as the fix is now MEDIUM+ (2 clean long runs, s3 and s5) but not proven. If a long job dies, read `supervisor.log` + `SUP_HEARTBEAT`: a stalled heartbeat with no "leg N exited rc=" line proves killed not crashed. The 3db4777 "engine death" diagnosis was WRONG (findings section 8).
- **`--ent-coef` was a silent no-op on `--resume` before `fb7ff99`.** `PPO.load` restores the checkpoint's saved `ent_coef`, so the flag was ignored while still being written into `summary.json` (a false report). `vzd_ppo_train.py` now reapplies it after load; verified `self.ent_coef` read as a plain float at `ppo.py:256` (SB3 at `C:\Users\maste\AppData\Roaming\Python\Python314\site-packages`). The s5 finetune is the payoff: the override is what made re-adaptation possible.
- **Seed a resume dir from the numbered checkpoint, never `model.zip`.** `_sibling_vecnormalize()` matches `_<N>_steps.zip` -> `_vecnormalize_<N>_steps.pkl`; `model.zip` matches nothing, so resuming from it re-inflates returns from std 1.0 and undoes the normalization fix. Supervisor `newest_ckpt` scans the out dir, so seed must be present and correctly named.
- **Standing caveats:** every decisive skill-3 and skill-5 result is a SINGLE SEED (n=1); seeds are owed before the README makes a transfer claim. `combat_probe.json` SHOTS_FIRED/accuracy are UNRELIABLE (AMMO2 baseline read at reset() inherits prior episode's terminal value); KILLCOUNT/HITCOUNT/DAMAGE_TAKEN clean, tic-trace is the accuracy source. vizdoom 1.3.0, sb3 2.8.0, Python 3.14. Eval is RAW so numbers compare across runs; shaped ep_rew_mean is NOT comparable to the flat curve. `runs\` is gitignored.
