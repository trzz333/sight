# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. K5.7 (QR-DQN + n-step, epsilon-greedy) 200k final eval is now ARMED to auto-fire on completion but the run is STILL RUNNING at session close. K5.8 (NoisyNet QR-DQN) smoke is armed to auto-fire after the K5.7 eval; the K5.8 200k launcher is staged behind a manual gate. K5.9 (DQfD-lite, demonstration-seeded replay) remains the named high-confidence fallback. From-scratch RL must beat the 930.27 constant-action baseline. PPO rejected K5.1-K5.5; PPO finetune rejected K5.6; vanilla DQN rejected (right-collapse); QR-DQN 50k ckpt FAIL (left-collapse). Single-voice governance.

**Last commit:** `6c379e1` K5 pipeline staging: arm post-run auto-eval / auto-smoke + K5.8 200k launcher

**Current task:** K5.7 200k QR-DQN run is STILL RUNNING detached, last observed ~139.7k/200k at 14:23 (loss ~626, in-training ep_len_mean ~547, exploration_rate floored 0.05), python PID 7460 + Godot PID 24992 alive and advancing. No completed 200k model yet; the 50k ckpt remains FAIL (mean 736.3, frac_R 0.000, left-collapse, anchored to runs\phase_k\k5_7_qrdqn_ckpt50k\eval_inenv\dqn_eval_inenv_report.json). Two things shipped this session. (1) Value-stability check on note 1c, done statically from tools\k5_7_qrdqn_train.py: QRDQN n_quantiles=200 with SB3-contrib sum-over-quantiles means the reported train/loss ~500-626 is ~2.5 per-quantile Huber = TD error ~3 on Q-values bounded near 100 (gamma 0.99, +1/step). A mean-over-quantiles loss of 500 would be impossible on bounded returns, which confirms the sum reading. So the loss-climb is measurement scaling, NOT value divergence; note 1c (reward-scale) is demoted to LOW (MEDIUM-HIGH confidence). (2) Staged and launched the post-run auto-pipeline: tools\wait_eval_k5_7.bat (detached, waits for runs\phase_k\k5_7_qrdqn\train_meta.json then runs the K5.7 final in-env eval, writes runs\phase_k\k5_7_qrdqn_eval_done.sentinel) and tools\wait_smoke_k5_8.bat (detached, waits for that eval sentinel then runs the K5.8 NoisyNet smoke 8000 steps to runs\phase_k\k5_8_smoke, writes runs\phase_k\k5_8_smoke_done.sentinel). Both launched detached this session, sequential by construction so only one Godot runs at a time. tools\run_k5_8_200k.bat (full K5.8 200k) is staged but NOT auto-fired. The wall is unchanged: cross-method single-direction collapse, read as exploration collapse in a provably L/R-symmetric env. Only above-baseline policies remain BC (1737.3) and PPO-finetune-from-BC (1710.5), both warm-started.

**Next action:** Run python tools\k5_poll.py, then check sentinels runs\phase_k\k5_7_qrdqn_eval_done.sentinel and runs\phase_k\k5_8_smoke_done.sentinel. (1) If the K5.7 eval sentinel is present, read runs\phase_k\k5_7_qrdqn\eval_inenv\dqn_eval_inenv_report.json (or k5_7_final_eval.log) for the K5.7 verdict (expect FAIL or degenerate). (2) If the smoke sentinel is present, read runs\phase_k\k5_8_smoke\k5_8_smoke.log: require sentinel body "DONE 0" AND finite non-NaN ep_len_mean. If clean, launch K5.8 200k: start "" /b cmd /c tools\run_k5_8_200k.bat (writes runs\phase_k\k5_8_noisy_qrdqn_200k.sentinel on done), then eval python tools\k5_8_noisy_eval_inenv.py --run runs\phase_k\k5_8_noisy_qrdqn --seeds 1000-1009 (PASS only if mean>=930.27 AND frac_L>=0.03 AND frac_R>=0.03 AND max(frac)<0.97). If the waiters did NOT fire (sentinels absent AND runs\phase_k\k5_7_qrdqn\train_meta.json absent), verify K5.7 process state (tasklist python.exe / Godot); if K5.7 died before the final save, run the K5.7 eval then the smoke manually. If the K5.8 in-env eval FAILs, go straight to K5.9 DQfD-lite (prefill the SB3 replay buffer with oracle/BC demonstration transitions, fixed expert:agent sampling ratio; Hester 2018); do NOT detour to reward-scaling (note 1c demoted). SIGHT_GODOT_EXE is set inside each .bat; set it inline for any manual eval. Wrap git in cmd /c "...".

**Blockers:** None requiring Jeff. Scope decision is informed and defaulted, not blocking (see note 5). Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable.

**Notes:**

- Escalation ladder (revised this session), in order. (a) K5.8 NoisyNet QR-DQN: staged, purity-preserving (zero demonstrations), changes the exploration MECHANISM not the epsilon constant. MEDIUM it clears the bar. (b) K5.9 DQfD-lite: prefill SB3 replay with oracle/BC demonstrations + fixed expert:agent sampling ratio (Hester 2018; textbook cure for value-RL collapse when a good policy provably exists, which it does, BC=1737). MEDIUM-HIGH, lowest risk, reuses on-disk assets. Asset VERIFIED this session: runs\phase_k\k5_6_bc\dataset_2000_2035.npz (64800 two-sided transitions L/Stay/R 25/50/25, 36 full-survival episodes, seeds disjoint from eval); build recipe + the obs-normalization parity gotcha in docs\k5_9_dqfd_notes.md. (c) reward-scale check DEMOTED to LOW: the loss-climb is sum-over-200-quantiles scaling, not divergence (TD error ~3 on Q~100). Do not retry QR-DQN harder; do not detour to reward-scaling unless both K5.8 and K5.9 fail.
- Auto-pipeline is ARMED but UNPROVEN through a full cycle: the two detached waiters chain K5.7-eval then K5.8-smoke off filesystem sentinels. Next session MUST verify the sentinels actually appeared (tool output), not assume the chain fired. If train_meta.json never appears, K5.7 crashed before its final save and the chain stalls harmlessly; run the eval and smoke manually.
- K5.8 staging remains UNPROVEN in-env. The selftest and loadtest prove only the noise mechanism and the save/load path. The K5.8 verdict comes solely from the in-env greedy eval on seeds 1000-1009 after a real 200k train. The smoke (8000 steps) only proves it runs against live Godot, not that it escapes the basin. Do not claim K5.8 works until that eval PASSes.
- Cross-method collapse is the central wall: PPO constant, DQN right-only, QR-DQN left-only, all in a provably L/R-symmetric env, so it is exploration collapse over the shared exploration/replay plumbing, not an env or value-overestimation problem. K5.8 attacks the exploration mechanism; K5.9 attacks replay contents. The honest from-scratch-RL demo still does not exist.
- SCOPE (Jeff-owned, informed, defaulted not blocking): "from scratch with ZERO demonstrations" is a handoff-only purity bar, NOT in the charter; the charter mission is already met by BC. K5.8 satisfies both; K5.9 DQfD-lite satisfies the charter but not the no-demos purity bar. Default: purity first (K5.8), DQfD-lite authorized fallback. reward_shaping "none" stays load-bearing (best constant 845.7 < bar 930.27); runs/ is gitignored. The K5.7 200k is a pre-reframe artifact (launched at e8f9bb8 before the symmetry finding); finishing it yields a clean completed-FAIL datapoint, not a method retry (one mid-run 50k ckpt FAIL is not two completed failures).
