# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. From-scratch reliability sweep RUNNING to N=10 (N=4 so far, still unreliable). Self-supervised / model-based track (K6) BUILT and smoke-verified this session; full run queued behind the sweep.

**Last commit:** `50da5e6` K6 built: NoisyNet QR-DQN plus self-supervised next-state prediction head on a shared encoder

**Current task:** The from-scratch reliability sweep (K5.8 NoisyNet QR-DQN, seeds 3-9 at 200k, train+eval chained, detached) is RUNNING; seed-3 finished this session, bringing N=4 (seeds 0,1,2,3). Reliability report at N=4: IQM 794.7, 95% bootstrap CI [645.9, 980.7] (straddles bar 930.27), frac_above_bar 0.25 (1 of 4), frac_nondegenerate 0.75; seed-3 scored about 848 (above best-constant 845.7, below bar). Still unreliable; verdict deferred to N=10. Separately, the self-supervised / model-based pillar was BUILT this session as K6: DynQRDQN (an sb3-contrib QRDQN subclass) adds a next-state prediction MSE (weight dyn_beta) summed into the quantile-Huber loss, on a shared learnable DynEncoder that feeds BOTH the NoisyNet quantile head and a prediction head, so the auxiliary loss shapes the trunk. Smoke PASS thread-capped alongside the live sweep: shared-encoder first-layer weights moved (delta L2 0.906, confirming gradients flow into the trunk) and dyn_loss fell 0.9665 to 0.4442 over 224 updates. dyn_beta=0 recovers K5.8 exactly. Method-to-env call: predict the next STATE directly, not latent-SPR (SPR's latent-target bottleneck only earns its complexity on pixels; deferred to H4). README splash rewritten (`36f1ebd`) for the current direction. found-art this session: "predict outcomes" generalizes to model-based RL / world models, verdict ADAPT (searches: "model-based RL world model 2025 Dreamer successor SOTA"; "self-predictive representations auxiliary task value-based RL SPR"); DreamerV3 (Hafner, Nature 2025) is the canonical full world-model agent, SPR (Schwarzer 2021) the lightweight bolt-on, K6 the state-env-appropriate adaptation.

**Next action:** Monitor the running seeds 3-9 sweep to N=10 (lean-poll <=45s; do not launch anything compute-heavy while it holds the CPU). As each seed finishes (writes seed.sentinel + eval_inenv\noisy_eval_inenv_report.json), re-run `python tools\k5_8_reliability_report.py` (auto-globs k5_8_noisy_qrdqn*) to recompute IQM + 95% bootstrap CI + frac_above_bar. At N=10 state the dependability verdict plainly against bar 930.27 (CI lower bound >= bar means reliably clears, claimable; CI straddles means unreliable, report frac_above_bar as the honest X/10; CI upper < bar means it does not clear at 200k). THEN, once the sweep frees the CPU, launch the K6 on/off comparison: DynQRDQN dyn_beta>0 vs dyn_beta=0 baseline, multi-seed at 200k, evaluated through the same eval + reliability machinery, to test whether self-supervised prediction lifts from-scratch reliability. Pick the next lever with a one-line reason: the K6 result plus the N=10 verdict decide whether K5.9 DQfD-lite (reliable margin) is still needed. Do NOT revisit epsilon-greedy or reward-scaling. Wrap git in cmd.

**Blockers:** One Jeff-owned DIRECTION question, ripening as N grows: once the N=10 reliability report is in, is "from-scratch RL clears the constant-action baseline on X/10 seeds, IQM Y [CI lo, hi], while imitation (BC 1737.3) and PPO-finetune (1710.5) clear it reliably" an acceptable portfolio headline, or does Jeff want a reliable from-scratch result (CI lower bound above bar) before claiming the mission? Claude's engineering (sweep now, K6 next, possibly DQfD-lite) runs regardless and feeds this; Jeff decides only the framing. Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable. No other blocker.

**Notes:**

- K6 self-supervised / model-based track is BUILT and smoke-verified (commit `50da5e6`): src\sight_agent\rl\dyn_encoder.py, src\sight_agent\rl\dyn_qrdqn.py, tools\k6_dyn_smoke.py. The dyn_head is auxiliary-only and is NOT in saved params; eval uses the greedy quantile head so this is fine, but a full-reproducibility training resume would need dyn_head saved (LOW priority). Full 200k K6 run HELD behind the sweep for CPU.
- FOUND-ART (predict outcomes): verdict ADAPT. The intuition generalizes to model-based RL / world models. DreamerV3 (Hafner et al., Nature 2025) is the canonical full world-model agent; SPR (Schwarzer et al. 2021) the lightweight self-predictive bolt-on. K6 adapts the idea to a low-dim STATE env by predicting the next state directly. Latent-SPR deferred to the pixel phase (H4).
- Reliability methodology stands: dependability is an across-seed property (Agarwal et al. 2021 / rliable; reimplemented numpy/scipy in tools\k5_8_reliability_report.py). At N=4: IQM 794.7, CI [645.9, 980.7] straddles bar, frac_above_bar 0.25. Six seeds (4-9) pending.
- WHY K5.8 WORKED (vs K5.7): only the exploration MECHANISM changed (NoisyNet replacing epsilon-greedy; identical net/env/reward/gamma). Do not revisit epsilon-greedy or reward-scaling. reward_shaping "none" is load-bearing (best constant 845.7 < bar 930.27).
- Reliable above-bar policies remain BC 1737.3 (imitation) and PPO-finetune 1710.5. K5.9 DQfD-lite (de-risk notes docs\k5_9_dqfd_notes.md, verified asset runs\phase_k\k5_6_bc\dataset_2000_2035.npz, 64800 transitions) is the reliable-margin fallback if from-scratch stays unreliable and K6 does not lift it. runs/ is gitignored, so eval report jsons live only on StrongerJr.
