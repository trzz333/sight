# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** H3 implementation. Step 8 (config `configs/rl/signal_dodge_ppo_h3.yaml` + config-to-factory plumbing) closed and validated. Step 9 (stub transport unit tests under the H3 plan implementation sequence) is next.

**Last commit:** `dca2af3` feat(rl): add signal dodge h3 config plumbing

**Current task:** H3 step 8 closed. `pytest tests/rl -v --tb=short` 111 passed in 11.48s (102 prior + 9 net new across `test_rl_config.py` and the new `test_h3_train_plumbing.py`). Ready for step 9.

**Next action:** Step 9 per H3 plan implementation sequence: add stub transport unit tests under `tests/rl/test_h3_godot_smoke.py` (the default-tier file named in plan section 8), exercising the Gym surface against a fake transport. Coverage required by the plan: observation space `(10,)` `float32`, `Discrete(3)` action space, `reset(seed=0)` returns `(obs, info)`, `step(1)` returns the Gymnasium 5-tuple, 10 steps run without protocol drift, forced collision -> `terminated=True`, forced timeout -> `truncated=True`, `close()` is idempotent. The existing `tests/rl/test_h3_godot_env.py` already covers most of this against a duck-typed `FakeTransport`; step 9 should consolidate the plan-section-8 acceptance into the dedicated smoke module rather than duplicate, and resist scope creep into a new fake protocol layer. Do not introduce the `live_godot` marker yet (that is step 10).

**Blockers:**

- Live Godot smoke (plan steps 10/12) still gated on the `live_godot` pytest marker. Step 8 produced no live coverage; the H3 train plumbing test monkeypatches `make_env` and the eval plumbing was not exercised end-to-end. Godot 4.6.2 binary on StrongerJr is already installed and parse-validated, so steps 10 and 12 are unblocked when scheduled.
- Claude Desktop GPU/driver crash on Jeff's primary box. Tracked in `C:\Projects\ops\claude-desktop-crash-ledger.md`. Operational only, not a Sight evidence blocker. Sight sessions run on standalone DC remote MCP (deviceId `64416a67-1bdb-42fc-bf1a-48f988e6901d`).

**Notes:**

- New module `src/sight_agent/rl/godot_config.py` owns YAML -> factory-kwargs resolution. Pure (no factory or stable_baselines3 imports). Precedence: explicit YAML > env var (`SIGHT_GODOT_EXE` / `SIGHT_GODOT_PROJECT`) > (project_path only) factory default `games/signal-dodge`. YAML values are not overridden by env vars at this layer or at the factory layer. Relative `env.project_path` values are resolved against the repo root via `Path(__file__).resolve().parents[3]` so `project_path: games/signal-dodge` works from any CWD. `resolve_godot_kwargs(cfg)` returns `{}` for non-Godot configs, so the train and eval plumbing can splat the result unconditionally.
- `train.py` now branches via two helpers `_build_train_env` and `_build_eval_env`; both consult `resolve_godot_kwargs` and pass `run_dir=str(artifacts.run_dir / "godot-train"|"godot-eval")` plus `godot_executable` / `project_path` to `make_env` for Godot ids. Gymnasium ids retain the H2 call shape with no extra kwargs (verified by a CartPole test that sets `SIGHT_GODOT_*` env vars and confirms they do not leak into `make_env`). The H2-era `smoke_check_env` probe is bypassed for Godot ids because it calls `gym.make(env_id)` directly, which would launch Godot; obs shape and `action_n` are filled from the plan-locked `Box(10,)` / `Discrete(3)` so `run_start` metadata stays populated.
- `evaluate.py` now reloads `<train_run_dir>/config_effective.yaml` for Godot evals via `_load_train_effective_config` and `_build_eval_env_for_train_run`, then resolves Godot kwargs from that config and passes them through `make_env` with `run_dir=<eval_dir>/godot-eval`. Missing `config_effective.yaml` or a recovered config that yields no Godot kwargs raises a clear error rather than guessing kwargs. Gymnasium evals are unchanged; the eval call shape for `CartPole-v1` is identical to H2.
- `configs/rl/signal_dodge_ppo_h3.yaml`: `env.id: godot:signal-dodge-v0`, `env.n_envs: 1`, `env.godot_executable: null`, `env.project_path: games/signal-dodge`, `env.max_steps: 1800`, `algo` set to SB3 PPO MlpPolicy on `cpu`, `train.total_timesteps: 1024`, `eval.eval_freq: 512`, `eval.n_eval_episodes: 2`, `logging.format: ndjson`, `checkpoint.enabled: true` with `filename: model.zip`. No machine-specific Godot binary path is committed; runtime resolves to absolute via SIGHT_GODOT_EXE.
- Tests added: `test_default_h3_godot_yaml_loads`, `test_is_godot_env_id_only_matches_exact_id`, four `resolve_godot_kwargs` cases (non-Godot returns empty, relative path resolves to repo-root absolute, env var fallback when YAML null, YAML overrides env var) in `test_rl_config.py`. Three plumbing cases in new `tests/rl/test_h3_train_plumbing.py` (Godot kwargs threaded into train call, distinct godot-train vs godot-eval `run_dir` per mode, CartPole call leaks no Godot kwargs even with env vars set). All assert by inspecting kwargs captured by a monkeypatched `make_env`; no Godot subprocess spawned, no live Godot launched, no `live_godot` marker introduced.
- HEAD progression: `a801d58` (handoff post step 6 hardening) -> `060d49c` (step 7 factory routing) -> `cf4c329` (handoff post step 7) -> `dca2af3` (step 8 config plumbing) -> handoff refresh (this commit). Default RL gate: 111 passed in 11.48s on StrongerJr. No live Godot path was touched in this step.
