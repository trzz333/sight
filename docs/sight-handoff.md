# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** H5 (Phase F frame-stack diagnostic sweep complete. Negative result: Phase G NOT triggered. Frame-stack observation contract validates mechanically but does not produce a non-degenerate policy at the Phase D entropy recipe and 10000-timestep budget.)

**Last commit on HEAD:** `4ae429f` docs(h5): phase F frame-stack diagnostic sweep evidence

**Substantive code commit:** `5b8fa48` feat(rl): h5 phase f frame-stack observation contract

**Current task:** Phase F diagnostic train sweep executed and analyzed. Frame-stack negative controls were rerun under `(4, 84, 84)` and passed non-saturation (`stay_only` length_ratio 0.337, `seeded_random` 0.230, `untrained_cnn` 0.207, all timeout_rate 0.0). Trained sweep over seeds {1, 2, 3} at 10000 timesteps each, eval over external seeds 1000-1009 with `trained_cnn` only: seed 1 (reward 844.8, length 845.7, collision 0.9, timeout 0.1), seed 2 (reward 688.8, length 689.7, collision 0.9, timeout 0.1), seed 3 (reward 605.0, length 606.0, collision 1.0, timeout 0.0). Pooled mean across train seeds: reward 712.87, length 713.80, collision 0.933, timeout 0.067. Phase E aggregate for comparison: reward 764.87, length 765.80, collision 0.933. Pooled Phase F is worse than Phase E on reward and length by ~52 each, identical on collision rate. Phase G triggers fail under either reading of "best frame-stack negative": collision_rate=0.933 misses the <=0.80 bar regardless of best-negative choice, reward/length miss the H5 25% gap against stay_only (target 756.25 / 757.50), and pooled reward/length are worse than Phase E. Failure mode is policy degeneration: seed 1 training log shows entropy collapse from iteration 33 (timestep 8192) onward with `approx_kl=0.0`, `entropy_loss in [-0.0005, -0.0012]`; seed 3 trained_cnn per-seed episode lengths are byte-identical to stay_only baseline `[333, 273, 843, 963, 1203, 1263, 543, 183, 183, 273]`, so seed 3 deterministic eval picks "stay" every step. Seed 3 train required one retry after a transient `GodotTransportError: recv timed out after 5.0s` at step 0; retry started from cleared run dir with same config_hash and completed `status=ok`. Frame-stack contract mechanical validation confirmed: seed 3 run_start.env_smoke.obs_shape=[4, 84, 84]. Phase F evidence written to `docs/h5-phase-f-frame-stack-evidence.md`. The H5 plan non-saturation gate uses `timeout_rate >= 0.50` OR `mean_episode_length >= 0.80 * max_steps` (unchanged by Phase F).

**Next action:** GPT to interpret the negative result and choose the next Phase F (or successor phase) lever. Options on the table: (a) raise `ent_coef` from 0.01 to delay collapse, (b) raise `total_timesteps` past the collapse-onset point to test recovery, (c) reshape per-step reward (currently +1/step survival, which incentivizes the stay solution), (d) change a different perception axis (e.g., color, pixel-grid size). Not a Claude decision. Two open ambiguities to resolve before next phase: (1) "best frame-stack negative" convention for Phase G trigger (hardest-to-beat = stay_only, vs lowest-collision = three-way tie at 1.0, vs lowest reward/length = untrained_cnn) - did not change the Phase F verdict, but should be locked; (2) handoff convention for `Last commit` field, currently split between HEAD commit and substantive code commit per Jeff's direction this session.

**Blockers:** none operational. Open: GPT to choose next experiment lever and resolve the two open ambiguities. Seed 3 train transient retry is reproducible from disk (saved `model.zip` matches the eval results) and not a blocker on its own; if it recurs on the next sweep, consider adding a startup retry loop to `godot_transport.py`.

**Notes:**

- Phase F evidence at `docs/h5-phase-f-frame-stack-evidence.md`. All metrics in the evidence doc are read from JSON artifacts in `runs/rl/signal_dodge_ppo_h5_pixel_frame_stack/`; no remembered or hand-calculated numbers.
- Phase F failure mode is policy degeneration, not a perception-contract failure. Frame-stack tensors propagate correctly through train and eval (verified via run_start.env_smoke.obs_shape and the seed 3 saved `model.zip` observation_space). The dominant blocker at 10000 timesteps is the stay-solution local optimum: survival reward +1/step + `ent_coef=0.01` is insufficient to keep the policy off the trivial stay action.
- Phase E aggregate (`mean_reward 764.87`, `mean_length 765.80`, `collision_rate 0.933`) was the comparison reference. Phase F pooled (712.87 / 713.80 / 0.933) is strictly worse on reward/length and equal on collision. The Phase F hypothesis "single-frame perception is the blocker" is neither falsified nor supported by this result; the dominant blocker is policy collapse, not perception.
- The H5 non-saturation gate at `docs/sight-h5-plan.md` section 5 still uses `timeout_rate >= 0.50` OR `mean_episode_length >= 0.80 * max_steps` to flag a saturated negative control; the frame-stack negative controls cleared this gate (max length_ratio 0.337, max timeout_rate 0.0).
- Seed 3 transient retry: initial run hit `GodotTransportError: recv timed out after 5.0s` at step 0 (Godot startup race). Retry from cleared run directory with the same seed, config, and config_hash completed `status=ok` on the second attempt. If this happens again on the next sweep, add a 3-attempt startup retry loop in `src/sight_agent/rl/godot_transport.py` rather than relying on manual retries.
