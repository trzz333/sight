# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. K5.7 (QR-DQN + n-step, epsilon-greedy) 200k final eval STILL PENDING (run detached, not finished). K5.8 (NoisyNet QR-DQN) STAGED and unit-verified but NOT yet trained or smoke-tested against live Godot. From-scratch RL must itself beat the 930.27 constant-action baseline. PPO rejected K5.1-K5.5; PPO finetune rejected K5.6; vanilla DQN rejected (right-collapse); QR-DQN 50k ckpt FAIL (left-collapse). Single-voice governance.

**Last commit:** `b243a91` chore: refresh handoff with K5.7 QR-DQN substantive hash e8f9bb8

**Current task:** K5.7 200k QR-DQN run is STILL RUNNING detached and unfinished at session close: sentinel runs\phase_k\k5_7_qrdqn_200k.sentinel ABSENT, last observed ~102.8k/200k steps, loss climbing monotonically (504 -> 580 over the session), in-training ep_len_mean ~497, exploration_rate floored at 0.05. The K5.7 final in-env eval (the prior Next action) therefore did NOT run; there is no completed 200k model to eval. The 50k checkpoint remains FAIL (mean 736.3, frac_R 0.000, left-collapse), anchored to runs\phase_k\k5_7_qrdqn_ckpt50k\eval_inenv\dqn_eval_inenv_report.json. This session used the CPU-starved window to verify a structural finding and stage the next escalation. Structural finding (HIGH, anchored to games\signal-dodge\scripts\main.gd / player.gd / hazard.gd this session): the env is provably left/right SYMMETRIC (player starts centered, symmetric speed 300 and bounds, hazard x uniform over full width, observation antisymmetric under reflection). So the cross-method single-direction collapse (PPO constant, DQN right-only, QR-DQN left-only) is an EXPLORATION pathology, NOT an env asymmetry, which kills the "inspect env for L/R symmetry" prong the prior handoff floated. found-art (web search: DQN constant-action collapse, CleanRL Rainbow/NoisyNet): epsilon-greedy is dithering exploration that cannot escape a directional basin; collapse coincides with action-value-gap contraction toward a low-quality attractor, not divergence. Verdict ADAPT: sb3-contrib 2.8.0 has QRDQN but no NoisyNet, CleanRL Rainbow is CNN-only, so adapt SB3 QuantileNetwork by swapping create_mlp for NoisyLinear (Fortunato 2017 factorised noise), epsilon disabled (all eps 0). K5.8 unit-verified TWICE this session against on-disk code: k5_8_noisy_selftest.py PASS (3 noisy layers, train_diff 0.206 noise-live, eval_diff 0.0 deterministic, learn ran); k5_8_loadtest.py PASS (save/reload predicts identically and deterministically). NOT tested: K5.8 against live Godot (a smoke run), because the 200k owns the i7-10750H.

**Next action:** Run C:\Projects\Sight\tools\k5_poll.py. If sentinel DONE, set SIGHT_GODOT_EXE inline then eval K5.7 final: python tools\k5_7_qrdqn_eval_inenv.py --run runs\phase_k\k5_7_qrdqn --seeds 1000-1009 (PASS only if mean >= 930.27 AND frac_L >= 0.03 AND frac_R >= 0.03 AND max(frac) < 0.97). Expect FAIL or degenerate (50k was left-collapse, loss climbing). On K5.7 FAIL, do NOT retry QR-DQN. Smoke-test K5.8 first: set SIGHT_GODOT_EXE inline, python tools\k5_8_noisy_qrdqn_train.py --timesteps 8000 --seed 0 --out runs\phase_k\k5_8_smoke, confirm exit 0 and train_metrics.ndjson populates finite ep_len_mean. If smoke clean, launch the full 200k detached via a .bat (python -u tools\k5_8_noisy_qrdqn_train.py --timesteps 200000 --seed 0 --out runs\phase_k\k5_8_noisy_qrdqn, redirect to .log, echo DONE %ERRORLEVEL% > .sentinel), poll with k5_poll.py-style lean checks. Then eval with tools\k5_8_noisy_eval_inenv.py. If the 200k is still running on resume, poll in lean checks (sleeps <= 45s); do not launch K5.8 while the 200k holds the CPU.

**Blockers:** None requiring Jeff. (Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable.)

**Notes:**

- K5.8 staging is committed but UNPROVEN in-env. The unit tests prove the noise mechanism and the save/load path, NOT that NoisyNet escapes the basin. That verdict only comes from the in-env greedy eval on seeds 1000-1009 after a real 200k train. Do not claim K5.8 works until that eval PASSes.
- The 200k QR-DQN run saturates the i7-10750H; the ndjson callback rewrites the whole file each rollout (truncate-then-write), so a naive reader can hit an empty file. k5_poll.py retries the read. Keep in-call sleeps <= 45s; the run is detached and survives poll timeouts.
- Cross-method collapse is the central wall: PPO constant (K5.1-5.5), vanilla DQN right-only (mean 695.7), QR-DQN 50k left-only (mean 736.3). Now established as exploration collapse in a symmetric env, attacked by changing the exploration MECHANISM (NoisyNet), not by raising epsilon (which is retrying epsilon-greedy harder, contract-forbidden).
- The only above-baseline policies remain BC (1737.3) and PPO-finetune-from-BC (1710.5), both warm-started. The honest from-scratch-RL demo still does not exist.
- reward_shaping "none" stays load-bearing: return = episode length, best constant 845.7 < bar 930.27. runs/ is gitignored; QR-DQN/K5.8 run artifacts and the .bat/.log/.sentinel are not committed. Only tracked tools, src, and this handoff persist.
