# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K (K3.5 complete and PASS: fixed reward-magnitude scaler at the VecEnv layer with `--reward-scale-divisor` divisors 30 and 100, standalone with `--value-bias-init none`, both pass primary AND strong gates cleanly. K3.5 PASS classification with /30 as confirmation candidate. The latent_vf collapse mechanism is fully prevented across the 2048-ts slice: `lvf_post = 128/128` across all 8 updates for both divisors. Absolute reward magnitude is load-bearing under Adam, not relative drift ratio. K1-extended remains parked).

**Last commit:** `bff9a6a` Phase K K3.5 fixed reward-scaling wrapper at divisors 30 and 100 passes primary and strong gates; latent_vf collapse fully prevented across 2048-ts slice.

**Current task:** K3.5 patched `tools/h5_training_entropy_probe.py` with `FixedRewardScaleVecEnv(VecEnvWrapper)`, `--reward-scale-divisor FLOAT` CLI flag (default 1.0 no-op), conditional wrapper application to the training VecEnv only (panel env NEVER wrapped), and header fields `reward_scale_divisor` and `reward_scale_applied`. Smoke (seed 3, 512 ts, /30) verified wrapper fires (`divisor=30.0 applied to train env only`) and header populates. Real /30 slice (seed 3, 2048 ts, pi=[64], vf=[128], `vf_coef=0.5`, `--value-bias-init none`): 8 updates, K-C verdict, elapsed 64.9 s. Primary gate PASS (lvf=128, vp_std=4.89e-3, post_ratio=3.66 < 22.84). Strong gate PASS (lvf=128, vp_std=5.33e-3, post_ratio=0.94 < 6.18). Real /100 slice (same params, divisor 100): 8 updates, K-C verdict, elapsed 64.8 s. Primary gate PASS (lvf=128, vp_std=6.33e-3, post_ratio=1.10). Strong gate PASS (lvf=128, vp_std=9.62e-3, post_ratio=0.75). `lvf_post` holds at 128/128 across all 8 updates for both divisors. No over-compression signal on /100 in the 2048-ts slice (`vp_std_post` 5e-3 to 1e-2, `post_ratio` 0.70 to 5.63 mostly under 1.3, `|g|` 0.29 to 1.89). Evidence in `docs/k3-5-reward-scaling-evidence.md`. CSV at `runs/phase_k/k3_5_reward_scale_table.csv`.

**Next action:** Run 10k confirmation on /30 per the final contract decision rule (/30 passes strong -> K3.5 PASS -> Run 10k confirmation on /30). Estimated wall time ~325 s based on 64.9 s for 2048 ts (39 PPO updates at the same n_steps=256). Use the same params: seed 3, pi=[64], vf=[128], `vf_coef=0.5`, `--value-bias-init none`, `--reward-scale-divisor 30`. Label `k3_5_confirm_reward_scale_div30_10000`. Gate the 10k slice on `latent_vf_live` holding through the full 39 updates and on `top_action_fraction` not crossing 0.95 (constant-action attractor check). If 10k confirmation passes, K3.5 closes and the next phase scoping is whether to revisit eval (the original Phase K anomaly: bit-identical eval results across distinct trained models) or to push training to a phase D-class 10k baseline with the wrapper.

**Blockers:** None requiring Jeff.

**Notes:**

- Absolute reward magnitude is load-bearing under Adam, not relative drift ratio. /30 ret_mean drift ratio is 4.91x (raw 14.12 -> 69.25) and /100 is 6.14x (raw 10.27 -> 63.03), both LARGER than K3.4 baseline 2.54x. Drift is preserved by construction, but the absolute per-update bias gap is reduced by the divisor. K3.5 /30 v_loss at update 2 is 0.064 vs K3.4 baseline 110 (1700x smaller). `|g|` at update 2 is 2.12 vs K3.4 128.82 (60x smaller). The cnn features extractor backprop now stays in a healthy range.
- The K3.4 secondary mechanism (drift chasing) is BOUNDED by absolute magnitude. K3.5 keeps the target moving (drift ratio larger than K3.4) but caps the absolute size of the chase per rollout. Adam absorbs the smaller catch-up without blasting upstream gradients into the shared features extractor. Fixed scaling changed the collapse mechanism, not just compressed the numbers.
- /100 also passes strong with no over-compression signal on this slice. The 2048-ts evidence does not support the over-compression suspicion (vp_std_post stays observation-conditioning, |g| non-trivial, top_action_fraction climbs normally). 10k confirmation on /30 still preferred per contract since /30 is the less aggressive scale and /100's slight v_loss compression near zero (0.002 to 0.10 vs /30 0.02 to 0.18) could matter at longer horizons.
- The K3.5 patch is additive and preserves all K3.x fields. Two new header fields (`reward_scale_divisor`, `reward_scale_applied`) and one VecEnv wrapper class. The wrapper applies only to the training env; the fixed observation-conditioning panel env is NEVER wrapped. Both /30 and /100 NDJSON headers confirm `reward_scale_applied=true`.
- Evidence and CSV layout: `docs/k3-5-reward-scaling-evidence.md` (K3.5), `runs/phase_k/k3_5_smoke_reward_scale_div30_512.ndjson` (smoke source), `runs/phase_k/k3_5_reward_scale_div30_2048.ndjson` (/30 source), `runs/phase_k/k3_5_reward_scale_div100_2048.ndjson` (/100 source), `runs/phase_k/k3_5_reward_scale_table.csv` (gitignored). Analysis script `tools/k3_5_extract.py` is preserved in the repo.
