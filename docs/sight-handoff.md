# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. K5.7 (QR-DQN + n-step, epsilon-greedy) 200k COMPLETE and FAILED its in-env eval. K5.8 (NoisyNet QR-DQN) 200k is LAUNCHED detached and RUNNING. K5.9 (DQfD-lite, demonstration-seeded replay) is de-risked and is the named fallback if K5.8 fails. From-scratch RL must beat the 930.27 constant-action baseline. PPO rejected K5.1-K5.5; PPO finetune rejected K5.6; vanilla DQN rejected (right-collapse); QR-DQN epsilon-greedy rejected (K5.7 200k FAIL). Single-voice governance.

**Last commit:** `d4ff34f` K5.9 DQfD-lite de-risk: verify demo asset (64800 two-sided transitions) + record build recipe and obs-norm gotcha

**Current task:** K5.7 200k QR-DQN is COMPLETE (sentinel DONE 0) and FAILED its final in-env eval on held-out seeds 1000-1009: mean 547.7 (bar 930.27, delta -382.6), collision_rate 1.0 (dies every episode), action fractions L/Stay/R 0.306/0.131/0.563 (anchored to runs\phase_k\k5_7_qrdqn\eval_inenv\dqn_eval_inenv_report.json, read this session). KEY UPDATE: this result is NON-degenerate (frac_L and frac_R both well above 0.03, max 0.563 < 0.97), so the 200k run ESCAPED the 50k ckpt's pure-left collapse (frac_R was 0.000) and became a two-sided-but-incompetent policy. The residual wall at 200k is therefore a COMPETENCE gap, not a frozen one-action basin: epsilon-greedy QR-DQN does diversify its actions given enough steps but never reaches dodging accuracy. The detached auto-pipeline fired END-TO-END this session with zero babysitting (K5.7 train -> auto K5.7 eval -> auto K5.8 smoke, all chained off filesystem sentinels). The K5.8 NoisyNet smoke PASSED its gate (exit 0, full 8000 steps, ep_len_mean 418 finite, exploration_rate 0 confirming NoisyNet-only, 42.9 fps, model saved; anchored to runs\phase_k\k5_8_smoke\k5_8_smoke.log). The K5.8 NoisyNet 200k is now LAUNCHED detached and RUNNING (python PID 12420 + Godot PID 5640 alive at 15:54, log dumping; seed-0-deterministic so its first 8000 steps match the smoke bit-for-bit, then diverge), and writes runs\phase_k\k5_8_noisy_qrdqn_200k.sentinel on completion (~3hr at ~13-19 fps). K5.9 DQfD-lite is de-risked (recipe in docs\k5_9_dqfd_notes.md). Above-baseline policies remain only BC (1737.3) and PPO-finetune-from-BC (1710.5), both warm-started.

**Next action:** Check runs\phase_k\k5_8_noisy_qrdqn_200k.sentinel. If DONE, eval the K5.8 NoisyNet policy: python tools\k5_8_noisy_eval_inenv.py --run runs\phase_k\k5_8_noisy_qrdqn --seeds 1000-1009 (SIGHT_GODOT_EXE is set inside run_k5_8_200k.bat; set it inline for a manual eval; PASS only if mean>=930.27 AND frac_L>=0.03 AND frac_R>=0.03 AND max(frac)<0.97). If the 200k is still running, lean-poll (sleeps <=45s); do NOT launch anything while it holds the CPU. On K5.8 in-env FAIL, build K5.9 DQfD-lite per docs\k5_9_dqfd_notes.md: verified asset runs\phase_k\k5_6_bc\dataset_2000_2035.npz, reconstruct (s,a,r=+1,s',done) per episode (all timeouts, drop the 36 boundary transitions), replace running VecNormalize with FIXED oracle-derived obs-norm stats, prefill the SB3 replay buffer, fixed expert:agent sampling ratio ~1:4, smoke 8000 then 200k detached, same eval bar. Do NOT detour to reward-scaling (note demoted to LOW). Do NOT retry QR-DQN epsilon-greedy (K5.7 200k FAIL closes it). Wrap git in cmd /c "...".

**Blockers:** None requiring Jeff. Scope decision is informed and defaulted, not blocking (see note 5). Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable.

**Notes:**

- K5.7 200k FAIL closes epsilon-greedy QR-DQN: mean 547.7, two-sided (L/Stay/R 0.306/0.131/0.563), collides every episode. The 200k escaped the 50k one-sided collapse, so the residual wall is COMPETENCE, not exploration-to-one-action. Do not retry K5.7 harder.
- The detached auto-pipeline proved out end-to-end this session: waiters chained train -> K5.7 eval -> K5.8 smoke off filesystem sentinels with zero babysitting (tools\wait_eval_k5_7.bat, tools\wait_smoke_k5_8.bat). Pattern works, reuse it for K5.9.
- K5.8 NoisyNet 200k is RUNNING (launched ~15:54, ~3hr). The smoke passing its gate (exit 0, ep_len_mean 418) only proves it RUNS against live Godot; the K5.8 verdict comes ONLY from the in-env greedy eval on seeds 1000-1009 after the full 200k. Do not claim K5.8 works until that eval PASSes.
- K5.9 DQfD-lite is de-risked and ready: verified asset (64800 two-sided full-survival transitions, seeds disjoint from eval), build recipe + obs-norm parity gotcha in docs\k5_9_dqfd_notes.md. MEDIUM-HIGH it clears the bar; lowest-risk rung, reuses on-disk assets. reward-scale (old note 1c) DEMOTED to LOW: the loss-climb is sum-over-200-quantiles scaling, not value divergence (TD error ~3 on Q~100).
- SCOPE (Jeff-owned, informed, defaulted not blocking): "from scratch with ZERO demonstrations" is a handoff-only purity bar, NOT in the charter; the charter mission is already met by BC. K5.8 (running) preserves purity; K5.9 DQfD-lite satisfies the charter but not the no-demos purity bar. Default: purity first (K5.8), DQfD-lite authorized fallback. reward_shaping "none" stays load-bearing (best constant 845.7 < bar 930.27); runs/ is gitignored. Only escalate the purity question to Jeff if BOTH K5.8 and K5.9 fail.
