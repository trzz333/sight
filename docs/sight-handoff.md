# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** H5 amended; Phase I docs-and-tools-only activation comparator complete. Two invocations loaded all four Phase E and Phase G pixel-CNN policies and queried the full SB3 actor path (`extract_features`, `mlp_extractor`, `action_net`, `value_net`, `get_distribution`) on the stay-tape and left-tape observation sequences at eval seed 1000. Total wall time 1m25s. Findings refine the Phase H "observation-insensitive" verdict: features DO vary across observations (effective rank 1.0 to 2.6, adjacent-step L2 0.08 to 0.86, first-vs-last L2 0.6 to 44.3) and raw action logits DO vary across observations (selected-action std 0.001 to 0.56), but the trained raw-logit margins are many times larger than the per-step projection variation so argmax never crosses. Class A (seed=1) sits in Branch C of GPT's pre-Phase-I scope note: selected-action projection std 0.011 to 0.020 against margins 6.5 to 11.1, margin-to-std ratio 540 to 630, the action head produces near-constant raw logits despite varying features. Class B (seed=2) sits in Branch D: selected-action projection std 0.05 to 0.56 against margins 0.83 to 6.31, margin-to-std ratio 4.5 to 41, the actor path varies substantively but argmax never flips. Phase E seed2 on the left tape is the smallest margin headroom across all eight model-tape cells (margin min 0.738, std 0.165, ratio 4.5). Branch E (tensor extraction mismatch) falsified at near-machine precision: max abs error between manual softmax and SB3 distribution probs is 1.19e-7. Branch B (encoder sees, actor latent collapses) is empty by construction in this configuration: default `CnnPolicy` `net_arch=None` resolves to `nn.Sequential()` for both `policy_net` and `value_net` so `latent_pi == latent_vf == features` bit-identically. Biases are operationally negligible (1e-3 to 4e-3) against projection means of 6 to 11; argmax is driven by `W @ latent_pi`, not bias gaps. Phase G reward shaping widened raw-logit margins 1.7x (Class A) to 5.1x (Class B) and grew the selected-action weight row norm 37% to 123% while keeping argmax fixed.

**Last commit:** `b1f5546` docs(h5): phase I activation comparator evidence + tool.

**Current task:** Phase I evidence at `docs/h5-phase-i-activation-comparator-evidence.md` (13 sections per GPT scope-note spec: TL;DR, why Phase H forces this slice, tool and methodology, model fingerprints carried forward, observation freshness rechecked, actor-path extraction validity, stay tape results, left tape results, cross-tape within-model comparison, action-net bias and weight inspection, branch-table verdict, next slice recommendation including Grok-prompt status, reproduction recipe). Tool at `tools/h5_activation_compare.py` is docs-and-tools-only: no training, no env code change, no config change, no reward variant. Reuses fingerprinting, tape parser, env factory, obs hashing from `tools/h5_logit_compare.py`. Per-tape summary JSONs at `runs/phase_i/activation_compare_{stay,left}.summary.json` plus Godot eval sidecars and driver log (all gitignored). Driver pattern matched Phase H: `start "title" /MIN cmd /c <bat>` plus sentinel poll.

**Next action:** GPT plans Phase J stochastic-action eval ablation per the Phase I evidence section 12 spec. Operationally meaningful only for Class B; Class A is saturated past stochastic recovery (G1 entropy 0.0002, E1 entropy 0.011). Phase E seed2 left tape predicted to surface non-`left` actions on ~38% of steps under categorical sampling (Phase H per-action probability mean left 0.62, right 0.27, stay 0.11; Phase I entropy mean 0.888 confirms). What stochastic eval would prove either way: if E2 terminal outcomes change under sampling, the deterministic-argmax surface was masking real trained-policy behavior in Class B and the fix path is eval-side; if E2 terminal outcomes do not change, the H5 plateau is structural in the env-policy coupling and the fix path is train-side. Jeff approval required before execution.

**Blockers:** none.

**Notes:**

- Branch verdicts split by basin. Class A (E1, G1): Branch C, action head emits near-constant raw logits (selected-action std 0.011 to 0.020) on margins 6.5 to 11.1. Class B (E2, G2): Branch D, actor path varies substantively but trained margin is too wide for any excursion to cross. Smallest headroom cell is E2 left tape, ratio 4.5.
- Default `CnnPolicy` `net_arch=None` collapses `latent_pi == latent_vf == features`. Branch B is empty by construction. The mlp_extractor's policy_net and value_net are `nn.Sequential()` (identity). The actor latent IS the encoder output for this configuration.
- Phase G reward shaping observable inside the network. Margins widen 1.7x to 5.1x; selected-action weight row norm grows 37% (E1 → G1 `W[stay]`) to 123% (E2 → G2 `W[left]`). Biases negligible (1e-3 to 4e-3). The argmax-fixing constant-classifier behavior in Class A arises from the action_net producing near-constant projection outputs despite varying features, not from a bias-dominated regime.
- Cross-tape within-model. Class A nearly invariant across stay vs left tape (raw margin changes < 2%). Class B shows large changes: E2 features norm std 2.5 → 8.5, raw margin 1.41 → 0.83. The seed=2 basin IS reading the screen but the response stays below the margin threshold to flip argmax.
- Phase I scope-note Grok prompt has NOT been relayed yet. Not blocking for Phase J. Recommended to relay before any future training-time gradient-flow probe into the seed=1 vs seed=2 margin asymmetry (E1 margin 6.5 vs E2 margin 0.83 at the same hyperparameters and same training timesteps is a Grok-trigger puzzle per charter).
