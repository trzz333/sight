# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K (K4.0 panel-logit mechanism diagnostic complete. Verdict **K4-D + K4-C** with a K4-E-adjacent panel-coverage caveat. On the 32-row scripted-prefix panel: K3.5c 2048 and K3.5c 10000 produce identical deterministic argmax ("left") on every row, raw logits drift between the two checkpoints at linf_max 0.2038 without crossing any decision boundary (K4-D eval-invisible logit drift). On K3.5c 10000 the action head's decision boundary is pinned to a single action with mean top1-top2 margin 0.376 across diverse latent_pi (K4-C). Fresh seed-0 init produces argmax "stay" 100% by ~0.0004 margin; the "left" preference forms in the first 8 PPO updates and is robust through update 156. K4-A (feature extractor uniformity) is falsified: latent_pi dim_std_mean grows from 0.005 at 2048 to 0.013 at 10000. The 32 panel rows collapse to ~5-8 effectively-distinct logit signatures across the 4 panel_seeds, so the K4.0 verdict does not by itself explain the bit-identical 10-seed external eval; that layer remains open and is the K4.1 target).

**Last commit:** `0f42a71` Phase K K4.0 panel-logit mechanism diagnostic: K4-D + K4-C with panel-coverage caveat (substantive). Chore handoff refresh follows in this push.

**Current task:** K4.0 panel-logit mechanism diagnostic per the GPT K4.0 scope. Built `tools/k4_panel_logit_probe.py` (~740 lines) that reuses `build_fixed_observation_panel`, `snapshot_policy_state`, and `snapshot_action_net` from `tools/h5_training_entropy_probe.py`. Probed three policy states on the same 32-row fixed observation-conditioning panel: `fresh_seed0_init` (CnnPolicy at seed 0 with zero training steps, constructed via a stub DummyVecEnv that matches the H5 pixel space, valid because SB3 seeds inside `_setup_model` after env attach), `k3_5c_2048` loaded via `PPO.load` (SHA-256 E14D1A12...), `k3_5c_10000` loaded via `PPO.load` (SHA-256 5664A12E...). Recorded row-level raw logits / softmax probs / argmax / top1-top2 margin per (model, panel_row) plus per-model summaries with action_net hash, row norms, biases, feature-chain diversity at raw_obs / cnn_features / latent_pi / latent_vf / action_logits / value_predictions. Computed pairwise inter-model logit deltas. Artifacts: `runs/phase_k/k4_panel_logit_probe.json` (120 KB), `runs/phase_k/k4_panel_logit_probe_panel_rows.csv` (22 KB), `docs/k4-panel-logit-mechanism-evidence.md` (292 lines). Wall time ~70 s end-to-end. Background launch via PowerShell `Start-Process -Wait -RedirectStandardOutput -RedirectStandardError -NoNewWindow -PassThru` inside an outer detached PowerShell, validated to survive the 4-minute MCP `interact_with_process` ceiling.

**Next action:** GPT scoping on K4.1. Load-bearing K4 follow-up is to replace the scripted-prefix panel with real obs captured from the actual K3.5c trained-only eval rollouts at seeds 1000-1009, then rerun the same logit probe on those eval obs. Tests whether the action head still pins one action 100% on the actual eval distribution. If it does, the bit-identical 10-seed eval is explained by every eval obs mapping to the same action on both checkpoints. If it does not, K4-E fires properly and the within-regime invariance mechanism shifts to "two checkpoints producing the same step-by-step action trace on eval despite different weights and despite eval obs that locally support different argmaxes." Cheaper secondary slice: K4.2 instruments the production trainer to record per-update panel argmax distribution to time the "stay" -> "left" flip in the first 8 updates.

**Blockers:** None requiring Jeff.

**Notes:**

- K4-A (feature extractor uniformity producing identical logits across panel obs) is falsified on K3.5c 10000: latent_pi dim_std_mean grows from 0.005 at 2048 to 0.013 at 10000, action_logits dim_std_mean reaches 0.014, and per-row logit range across the panel grows from 0.024 to 0.039. The feature pipeline is not collapsing; the action head is doing the pinning. K4-F (init-surface persistence) is also falsified: fresh init argmax is "stay" 100% while both K3.5c checkpoints are "left" 100% (0/32 argmax match between fresh and either checkpoint).
- Panel coverage caveat (K4-E-adjacent): 32 nominal rows collapse to ~5-8 effective logit signatures because the 4 panel_seeds produce identical logits for every scripted prefix except `initial`, and `initial` rows differ only at the 0.001 logit scale. The scripted prefixes converge the visible observation to a similar player+hazard configuration within 15-30 steps regardless of reset seed. The K4-D verdict is therefore confined to "logit drift on a narrow panel" and does not establish how the two checkpoints produce bit-identical eval on a broader 10-seed rollout distribution. K4.1 with an eval-obs panel is the necessary next probe.
- Fresh seed-0 action_net is at SB3's default near-zero init scale (row norms all 0.0100, biases all 0.0, mean logits ~0.001 across actions). "stay" wins on all 32 panel rows by margin ~0.0004, decided at numerical-noise scale rather than by structural design. Three candidate mechanisms for the "left" preference forming during early training (early-trajectory accident, spawn-configuration asymmetry, argmax-init lottery) are not pinned down by this probe.
- The `SIGHT_TCP_IGNORE_DEATH` refusal guard was not exercised in this session and remains intact in `run_p3_eval.py`. No training was run, so no death-tolerant branch was needed.
- Background-launch pattern update: write a wrapper .ps1 that uses `Start-Process -Wait -RedirectStandardOutput -RedirectStandardError -NoNewWindow -PassThru` for the Python child and writes a done-sentinel on exit, then launch the .ps1 from cmd via `powershell -NoProfile -Command "Start-Process powershell -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File','<path>' -NoNewWindow -PassThru"`. The outer Start-Process returns immediately; the inner -Wait blocks the wrapper PowerShell until python exits. This pattern handled the ~70 s K4 probe cleanly. The `cmd /c bat-with-stdout-redirect` pattern is still on the do-not-use list per K3.5c findings.
