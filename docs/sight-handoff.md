# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K (K5.1 alpha=0.30 high-force clearance reward executed and FAILED on the GPT FAIL clause "another constant-action basin"; constant-action regime shifted from K3.5c constant-LEFT to K5.1 constant-STAY; the next ladder rung per GPT K5.1 scope item 7 is K5.2-grok env-dynamics sanity check)

**Last commit:** `1db19a9` Phase K K5.1 alpha=0.30 high-force clearance reward: constant-action regime shift, FAIL

**Current task:** K5.1 evidence on disk in `docs/k5-1-clearance-alpha030-visible-behavior-evidence.md` (271 lines) and pushed. Tracked artifacts: new config `configs/rl/signal_dodge_ppo_h5_pixel_entropy_shaped_alpha030.yaml` (reward_shaping_alpha 0.30, otherwise identical to Phase G shaped config), modified `tools/h5_smoke_parse.py` (alpha-parametric via `--alpha` CLI flag, default 0.05 backward-compatible, saturation threshold normalized to 0.98*alpha, mean-bonus range gates scaled to [0.10*alpha, 0.90*alpha] and [0.20*alpha, 0.90*alpha]). Untracked: smoke driver `runs/smoke/run_k5_1_smoke.bat`, train driver `runs/rl/run_k5_1_train.bat`, eval driver `runs/rl/run_k5_1_eval.bat`, all live in gitignored dirs. Trained checkpoint at `runs/rl/signal_dodge_ppo_h5_pixel_entropy_shaped_alpha030/k5_1_alpha030_seed0_10k/model.zip` (10k steps, seed 0, reward-scale-divisor 30, 388 s training, exit 0). Eval at `runs/rl/signal_dodge_ppo_h5_pixel_entropy_shaped_alpha030/k5_1_alpha030_seed0_10k_trained_only/` (seeds 1000-1009, deterministic trained_cnn, mode full, exit 0). All 6060 reached eval steps emitted `action=0 (stay)` with `player_x=360.0` exactly; mean episode length 606 vs K3.5c 845.7; collision_rate 1.00; aggregate trained-policy frac_active_threat_saturated_norm = 0.263 < 0.50 (Phase G monitoring requirement met). K4.1-style logit-margin probe deferred (behavioral evidence is unambiguous binary FAIL).

**Next action:** GPT or Grok scopes K5.2-grok env-dynamics sanity check covering action timing per Godot physics tick, hazard kinematics, observation freshness across the H3 transport boundary, frame-stack contract, and player kinematics. Two anchored facts for the probe to explain: same K3.5c-divisor30 seed=0 regime produces constant-LEFT at the left wall under `reward_shaping=none` and constant-STAY at center under `threat_weighted_clearance` alpha=0.30. Both are deterministic-argmax single-action fixed points at 10k steps / 40 PPO updates. Whatever env-layer property forces single-action collapse must explain both attractor positions.

**Blockers:** None requiring Jeff. K5.1 fell inside Claude's charter execution authority; the smoke-gate per-episode saturation FAIL was revised to the Phase G aggregate disposition on evidence; the divisor=30 tactical fill-in for the unspecified divisor parameter was Claude's call to keep K5.1 in the K4.1 reference regime.

**Notes:**

- The smoke-gate FAIL pattern is structural and alpha-invariant: `clearance_norm >= 0.98` is the saturation predicate, alpha scales bonus magnitude not the saturation indicator, so ep-000005 saturation = 0.524 is byte-identical between Phase G (alpha=0.05) and K5.1 (alpha=0.30) smokes. Aggregate frac_sat across the representative sample was 0.416 < 0.50 in both. Phase G disposition carries forward unchanged.
- K5.1 produced visible policy-level change (different argmax) without visible behavior-level change (still single-action, still 100% collision, shorter mean episodes than K3.5c). The constant-stay basin at x=360 is the basin maximum for `threat_weighted_clearance` reward in this env (center maximizes minimum-clearance distance from off-axis hazards). Stronger alpha would push deeper into this exploit, not out of it.
- The K5.0 evidence doc already flagged K5.0-grok env-dynamics as the only unexercised ladder rung. K5.1 closes the K5.0-alt path with a specific failure mode; K5.2-grok is now the only remaining lever in the original section-7 ladder.
- Untracked artifacts under `runs/rl/` and `runs/smoke/` (driver bats, training run dir, eval run dir, smoke run dirs) are preserved on disk; gitignored so the repo stays clean. The K5.1 smoke configs (`runs/smoke/k5_1_smoke_shaped_alpha030.yaml`, `runs/smoke/k5_1_smoke_default.yaml`) are likewise gitignored.
- `tools/k4_1_eval_obs_logit_probe.py` is hardcoded for two K3.5c checkpoint inputs and would need a single-checkpoint extension before being usable on the K5.1 trained model; deferred and noted in the K5.1 evidence doc section 9.
