# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase N (from-scratch RL via structurally-distinct paradigms). C1 = CMA-ES CLOSED NEGATIVE. C2 = CMA-MAE CLOSED NEGATIVE. C3 = reward-ranked elite-BC (self-imitation, critic-free) ACTIVE, seed-0 training + autonomous screen armed. This is the last of the 3 pre-registered paradigms; if C3 fails its reliability gate, Phase N closes FINAL NEGATIVE.

**Last commit:** `e028963` Phase N C3: reward-ranked elite-BC trainer + autonomous seed screen + windowless status UI

**Current task:** C3 (reward-ranked elite-BC, the critic-free Cross-Entropy-Method trained-policy variant) built and running. `tools\c3_elitebc_train.py` rolls the current policy STOCHASTICALLY (softmax sample + epsilon 0.05), keeps the agent's own top-20% episodes by survival length, and backprops cross-entropy on their (s,a) directly into the SB3 actor, emitting a gate-native `best_actor_vec.npy` (5059 params, raw obs, argmax) so `tools\c1_es_eval.py` runs UNCHANGED. Critic-free VERIFIED off-disk: 6 actor tensors grad-on, 6 value tensors frozen. Throughput smoke PASS anchored to `runs\phase_n\c3_smoke\c3_report.json`: 95.8s/iter, 37.6 iters/hour; random-init rollout already action-diverse (L/S/R 0.22/0.28/0.50, no collapse). Seed-0 training live (detached, 60 iters, bc_epochs 10): anchored to `runs\phase_n\c3_screen_s0\c3_history.json` at iter 12, dev-best (argmax on dev band 300000-300007) climbed 372.4 -> 625.6, latest dev mix balanced 0.29/0.42/0.29 max_frac 0.424, still below the 930.27 bar (expected early). Autonomous pre-registered screen armed via `tools\c3_screen_orchestrator.py` (detached): waits seed-0's report, gates BOTH vectors (`best_actor_vec`, `best_final_vec`) on held-out 1000-1009, stages seeds 1+2 iff seed-0 best held-out mean >= 880.0 (near-miss, sits between C1 906.4 and C2 845.7), reliability gate = 3/3 seed passes. Live status UI at http://localhost:8766 (windowless).

**Next action:** Check screen state FIRST. Read `runs\phase_n\c3_screen_all.sentinel` and `runs\phase_n\c3_screen_verdict.json` (or open http://localhost:8766). If the sentinel exists: write `docs\phase-n-c3-findings.md` from the verdict and act on `screen_result`. C3-PASS-reliable-3of3 = first genuine from-scratch mission success (BC 1737.3 did not count); write it up. Any C3-NEGATIVE-* = Phase N closes FINAL NEGATIVE per the 3-paradigm stopping rule; write the closing finding. If the screen is still running, monitor to completion, do NOT relaunch (seed-0 and orchestrator are detached; liveness = `c3_history.json` mtime + Godot worker count, never the launcher pid). If seed-0's `c3_report.json` exists but the orchestrator is dead (no verdict, activity frozen): run `tools\c1_es_eval.py --vec runs\phase_n\c3_screen_s0\best_actor_vec.npy --seeds 1000-1009 --out ...` and the same for `best_final_vec`, apply the >=880 rule, then either restart `tools\launch_c3_orchestrator_detached.py` or stage seeds with `tools\launch_c3_detached.py <seed> 60 10`. Commit the screen artifacts as evidence into `docs\phase-n-c3-findings.md` (runs\ is gitignored).

**Blockers:** None requiring Jeff. C3 is a technical build within Claude's authority. (Optional, unchanged: NSSM/Scheduled-Task reboot-recovery needs Jeff's Windows password; the no-reboot overnight case is already solved.)

**Notes:**

- Mechanism is load-bearing and structurally distinct: C3 selects EPISODES (dense per-state BC targets from the agent's own filtered elites), not weight vectors (C1/C2) and not a critic+policy-gradient stack (M/L). Stochastic rollout is REQUIRED: argmax rollout + BC-on-own-argmax is an instant fixed point (zero learning signal). No external/scripted/human demos, so a C3 clear counts as mission success. found-art ADAPT (SIL 1806.05635, CEM elite-BC De Boer 2005), verified prior session; reused C1 policy plumbing + BC loop; NOT canonical SIL, NOT CEM-over-weights.
- Wall attacked: C1/C2 selected on ~2 noisy seeds per generation (the diagnosed high-variance-selection wall). C3 selects elites from a 48-episode batch per iteration (24x the selection base) and the update is dense supervised targets, not scalar fitness.
- Autonomous screen: `tools\c3_screen_orchestrator.py` detached, 6h per-seed wait deadline, seeds run SEQUENTIALLY so only one 8-worker Godot pool is live at a time. Outputs `runs\phase_n\c3_screen_verdict.json` + `c3_screen_all.sentinel`. Launcher pid != interpreter pid (venv python is a shim); do not track liveness by the printed launcher pid.
- Status UI: `tools\c3_status_server.py` via `tools\launch_c3_status_detached.py`, http://localhost:8766, stdlib-only, windowless (CREATE_NO_WINDOW), base `C:\Python314\python.exe`. Shows active seed, iters x/60, seed + best-effort screen ETA, dev action-mix collapse check, 3-seed held-out ledger with PASS/FAIL, final verdict banner.
- Eval gate UNCHANGED: held-out 1000-1009, PASS = mean>=930.27 AND frac_L>=0.03 AND frac_R>=0.03 AND max(frac)<0.97. `runs\` is gitignored: screen histories, eval summaries, and the verdict are NOT committed; fold them into `docs\phase-n-c3-findings.md` when the screen closes.
