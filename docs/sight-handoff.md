# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase N (from-scratch RL via structurally-distinct paradigms). C1 = CMA-ES CLOSED NEGATIVE. C2 = CMA-MAE CLOSED NEGATIVE (all 3 seeds, both vectors, held-out). One paradigm shot remains: C3 = reward-ranked elite-BC (self-imitation family, critic-free; NOT canonical SIL). On NLDC (hostname MSI, user maste). Phase M closed FINAL NEGATIVE.

**Last commit:** `8a9cad4` Phase N C2 (CMA-MAE): screen CLOSED NEGATIVE; decide C3 = self-imitation (this handoff refresh is the following chore commit)

**Current task:** C2 fully closed. All 6 held-out evals FAIL, anchored to `runs\phase_n\c2_screen_s*\eval_{actor,mean}\c1_eval_summary.json`: seed0 actor 523.0 / mean 845.7; seed1 actor 689.7 / mean 586.6; seed2 actor 845.7 / mean 704.7. Best held-out 845.7, below C1's 906.4 and the 930.27 bar. Verdict written to `docs\phase-n-c2-findings.md`. Cross-paradigm finding: C1 and C2 (both black-box weight search over the actor) fail identically on held-out; train seeds rotate per generation so it is NOT fixed-seed overfitting; the wall is return-only selection on ~2 noisy seeds in a high-variance env.

**Next action:** Build and run C3 = reward-ranked behavioral cloning on the agent's own filtered elite trajectories, iterated (the critic-free Cross-Entropy-Method trained-policy variant; conceptual ancestor SIL Oh 2018 arXiv 1806.05635, but NOT canonical SIL). found-art verdict ADAPT (verified 2026-07-03; SIL 1806.05635, CEM elite-BC De Boer 2005 / Lapan): reuse the existing BC trainer + Godot rollout/worker pool + the UNCHANGED held-out gate (`tools\c1_es_eval.py`, mean>=930.27 AND frac_L>=0.03 AND frac_R>=0.03 AND max(frac)<0.97 on seeds 1000-1009); add only a self-filtering outer loop: roll out across rotated seeds -> keep the agent's own top-k percent episodes by return (no external/scripted/human demos) -> BC on their (s,a) -> repeat. Mechanism choice is load-bearing: do NOT use canonical SIL (needs an A2C/PPO base + value critic, the exact stack Phase M and Phase L already closed NEGATIVE) and do NOT use CEM-over-weights (a third black-box weight search, same family as C1/C2, forbidden by method-fails-twice). Elite-BC is distinct from C1/C2 (dense per-state supervised target from filtered episodes, not scalar fitness) and from M/L (no critic, no policy gradient). It is from-scratch (a C3 clear counts as mission success where BC 1737.3 did not, because no known-good controller is handed to the agent). Run the pre-registered seed screen (seed 0 first; if it clears or near-misses, stage seeds 1+2 via the proven detached-chain pattern). Verify HARD against the reliability gate before any PASS claim. If C3 fails its gate: Phase N closes FINAL NEGATIVE per the 3-paradigm stopping rule, documented result = on this env from-scratch return-only learning does not reliably clear the baseline while imitation of any good action signal does.

**Blockers:** None requiring Jeff. C3 is a technical/tactics build, within Claude's authority. (Optional, unchanged: NSSM/Scheduled-Task reboot-recovery needs Jeff's Windows password; no-reboot overnight case already solved.)

**Notes:**

- C2 infra committed and proven this session: `tools\chain_c2_eval.py` (detached sentinel-triggered eval chain, ADAPT of `chain_c2_seed2.py`) fired all 4 evals sequentially on seed-2's sentinel with zero pool contention. Eval spins ONE Godot env (not a pool), and episode length + action fractions are step-deterministic given seed+weights, so eval metrics are load-independent; sequencing was for safety of the live unattended run, not correctness.
- Method-fails-twice CONFIRMED for black-box weight search: C1 (CMA-ES) and C2 (CMA-MAE) both fail lateral-action collapse / sub-bar on held-out. C2's QD diversity worked on TRAIN (166-202 archive cells) but did not transfer; best held-out (845.7) was below C1's (906.4). Do NOT run a third weight-search variant. C3 attacks the action signal, not the optimizer.
- Load-bearing project finding (grounded this session against `docs\phase-l-offline-rl-findings.md` / `k5_8_reliability_report.json`): the decisive variable is ACTION-SIGNAL quality, not the optimizer. Every good-signal method clears (BC 1737.3, PPO-finetune 1710.5); every from-scratch return-only method does not (PPO Phase M, NoisyNet K5.8 IQM 729.2 with 1/10 seeds above bar, offline Phase L, C1, C2). NoisyNet's single lucky seed (980.7) never became reliable. C3 (elite-BC self-imitation) is the direct test of whether a from-scratch method can MANUFACTURE its own good signal.
- WINDOWLESS DURABLE EXECUTION still PROVEN (C2 seeds 1+2 + eval chain all ran unattended this session, sentinels EXIT 0). Pattern unchanged: supervisor DETACHED_PROCESS (pool-less) + trainer/eval CREATE_NO_WINDOW + FOR_DISABLE_CONSOLE_CTRL_HANDLER=1 + PYTHONNOUSERSITE=1. Tooling note: MCP tool calls that run a silent process to completion trip the client's ~4-min ceiling; use a heartbeat-emitting bounded poll or a detached sentinel-writer + instant polls (both used successfully this session).
- Eval gate unchanged: held-out 1000-1009, PASS = mean>=930.27 AND frac_L>=0.03 AND frac_R>=0.03 AND max(frac)<0.97. Status server (if needed): `C:\Python314\python.exe tools\launch_status_detached.py`, http://localhost:8765.
