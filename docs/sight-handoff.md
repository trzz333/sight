# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** Phase K. REPRODUCIBILITY IN QUESTION. K5.8 (NoisyNet QR-DQN) from-scratch DID NOT reproduce across seeds. seed-0 PASSED (mean 980.7 > bar 930.27); seed-1 FAILED (mean 645.9 < bar, delta -284.4), both two-sided non-degenerate. So the single-seed from-scratch PASS was at least partly variance: the method clears the bar sometimes, not reliably. seed-2 200k is RUNNING detached to get N=3 and an honest across-seed spread before any locked claim. K5.9 DQfD-lite remains deprioritized but is now the live option if the from-scratch result proves unreliable. K5.7 (QR-DQN epsilon-greedy) closed FAIL. PPO/PPO-finetune/vanilla-DQN all rejected. Single-voice governance.

**Last commit:** `2677c7f` chore: refresh handoff with K5.8 PASS substantive hash 9399304 (PRE-DATES the seed-1 FAIL recorded this session)

**Current task:** Seed-1 reproduction of K5.8 NoisyNet from-scratch COMPLETED and FAILED this session. seed-1 200k finished clean (sentinel DONE 0, model qrdqn_noisy_sb3.zip written, log "DONE wall=11420.5s fps=17.5"); its in-env eval on held-out seeds 1000-1009 returned VERDICT FAIL: mean 645.9 (bar 930.27, delta -284.37), collision_rate 1.0 (10/10 deaths, zero timeouts), action fractions L/Stay/R 0.155/0.608/0.237 (two-sided, non-degenerate, but worse dodging than seed-0). Anchored to the REPORT json printed by tools\k5_8_noisy_eval_inenv.py this session. So the from-scratch result does NOT reproduce: seed-0 PASS 980.7, seed-1 FAIL 645.9. The single-seed PASS was variance-inflated. Honest current standing of from-scratch NoisyNet: 1 PASS / 1 FAIL straddling the bar; not yet a reliable result. seed-2 200k LAUNCHED detached this session (tools\run_k5_8_s2_200k.bat, --seed 2 --out runs\phase_k\k5_8_noisy_qrdqn_s2, python PID 7292 confirmed alive and writing train_metrics.ndjson, ~3hr), writing runs\phase_k\k5_8_noisy_qrdqn_s2_200k.sentinel on completion. N=3 will give the across-seed mean +/- spread needed to characterize this honestly. Reliable above-baseline policies remain BC 1737.3 (imitation) and PPO-finetune 1710.5; from-scratch RL is the only "pure" route and it is currently unreliable at 200k.

**Next action:** Check runs\phase_k\k5_8_noisy_qrdqn_s2_200k.sentinel. If DONE, eval the seed-2 model: set SIGHT_GODOT_EXE inline, python tools\k5_8_noisy_eval_inenv.py --run runs\phase_k\k5_8_noisy_qrdqn_s2 --seeds 1000-1009 (PASS only if mean>=930.27 AND frac_L>=0.03 AND frac_R>=0.03 AND max(frac)<0.97). Then report the full N=3 spread honestly: seed-0 980.7 PASS, seed-1 645.9 FAIL, seed-2 <result>; give mean and range across the three seeds and a plain verdict on reliability (how often 200k clears the bar). DECISION RULE for the verdict: if seed-2 PASSes -> 2/3 above bar, call it "marginal/unreliable but real" (clears often, not always), and the harden-vs-polish-vs-done fork goes to Jeff. If seed-2 FAILs -> 1/3 above bar, the single PASS was largely luck; from-scratch NoisyNet at 200k does NOT reliably clear the bar. In that case the honest options are (a) more steps / longer training to see if the bar is cleared reliably with more compute, or (b) accept BC/PPO-finetune as the only reliable above-baseline policies and reframe the from-scratch result as "achievable but not yet reliable", or (c) revive K5.9 DQfD-lite to push a reliable margin. Pick among a/b/c with a one-line reason, do NOT hand Jeff a menu; only the direction question (is an unreliable-but-real from-scratch result good enough for the portfolio claim) is Jeff-owned. If the seed-2 200k is still running, lean-poll (sleeps <=45s); do NOT launch anything while it holds the CPU. Do NOT revisit epsilon-greedy QR-DQN or reward-scaling. Wrap git in cmd /c "...".

**Blockers:** None hard. The DIRECTION fork (harden/polish/call-it) is PREMATURE: the hard mission is NOT cleanly met (seed-1 reproduction FAILED; from-scratch is 1/2 above bar, unreliable). Do not surface a harden/polish/done decision to Jeff until seed-2 lands and the N=3 reliability spread is known. Do not unilaterally expand scope. Repo github.com/trzz333/sight still PUBLIC; Jeff decided acceptable.

**Notes:**

- REPRODUCIBILITY CORRECTION + DIRECTION: the prior "milestone met" claim was single-seed and did NOT hold up. from-scratch NoisyNet QR-DQN at 200k: seed-0 PASS 980.7, seed-1 FAIL 645.9 (both two-sided). 1/2 above bar, straddling it. The hard mission is NOT cleanly met; it is "achievable on some seeds, unreliable." seed-2 running for N=3. Do not make any locked portfolio claim of a from-scratch PASS until the spread is in. The harden/polish/call-it fork stays Jeff-owned but is premature until reliability is characterized.
- WHY IT WORKED (vs K5.7): only the exploration MECHANISM changed (NoisyNet replacing epsilon-greedy; identical net_arch, env, reward "none", gamma 0.99). epsilon-greedy QR-DQN went two-sided-but-incompetent (547.7 FAIL); NoisyNet's consistent within-episode perturbation produced competent dodging (980.7 PASS). Confirms the wall was the exploration mechanism, not env/value/reward geometry. The "change the method, do not retry harder" rule paid off. Do not revisit epsilon-greedy or reward-scaling.
- K5.9 DQfD-lite DEPRIORITIZED: it was the fallback for pure-RL failure; pure RL succeeded. The de-risk notes (docs\k5_9_dqfd_notes.md, verified asset runs\phase_k\k5_6_bc\dataset_2000_2035.npz) remain on disk if ever wanted to push the margin well above the bar, but it is not needed for the charter or the purity bar.
- The detached waiter/sentinel pattern carried the full K5.7->K5.8 chain unattended last session and the K5.8 200k -> eval -> seed-1 repro launches this session. Reliable; standard tool now (tools\run_k5_8_eval.bat, tools\run_k5_8_s1_200k.bat added this session).
- SCOPE/honesty for any writeup: K5.8 is GENUINE from-scratch RL (no demonstrations, NoisyNet exploration); BC is imitation learning. Do not conflate the two. The from-scratch claim is currently single-seed (N=1 training seed, 10 eval seeds); state N and reproducibility status honestly. reward_shaping "none" stays load-bearing (best constant 845.7 < bar 930.27). runs/ is gitignored.
