# Sight - Session Handoff

Canonical handoff document. Updated at the end of every session by whoever executed. Target cold-start resume time: 10 minutes.

---

**Phase:** H5 amended; Phase J stochastic-action eval ablation complete. Two-stage workflow per GPT scope note. J0 pilot (Phase E seed2, 10 eval seeds, 5 replicates per seed = 50 episodes) crossed three of four applicable expansion-threshold criteria (mean length change 22.3%, collision rate change 0.10 at boundary, non-wall-hugging-class 100%; per-seed terminal-diff was 1/10 below the 3/10 threshold; player_x criterion not applicable since reward_shaping=none in this config does not emit `info["reward_state"]`). J1 expansion (Phase G seed2, same 50-episode shape) ran on the same protocol. Class A negative controls (E1, G1) skipped per scope note "only if budget is still clean"; Phase I entropy 0.011 (E1) and 0.0002 (G1) predict operationally indistinguishable stochastic-vs-deterministic outcomes. Total Phase J wall time J0+J1 = 35 min on StrongerJr.

Headline findings. Phase E seed2 stochastic eval differs from argmax on 36.0% of steps (Phase H predicted ~38% from per-action probability mean left 0.62 right 0.27 stay 0.11). Mean episode length drops 22% from deterministic 845.7 to stochastic 657.3. Collision rate rises from 0.90 to 1.00. Seven of ten seeds produce bit-identical episode lengths across all five replicates despite 36% non-argmax sampling because the env-terminal hazard is geometry-determined at specific frames. Three seeds (1000, 1002, 1008) show wide replicate variance. **Seed 1008, the deterministic 1800-step timeout survival, becomes 5-of-5 collisions under stochastic with lengths 513, 513, 517, 519, 1744**. The seed-1008 "survival" was a fragile commitment artifact of the wedge-at-x=16 strategy, not learned dodging. Phase G seed2 stochastic eval differs from argmax on 1.4% of steps; every per-seed replicate length matches deterministic bit-identically; Phase G's reward shaping sharpened the distribution to operationally zero sampling effect.

Verdict closest to J-B (actions change but trajectory class largely unchanged) with sharpened reading: deterministic argmax was NOT masking learned competence (J-A falsified). Phase G's shaping tightened commitment to the wedge rather than teaching dodging. **The H5 plateau is structural in the env-policy coupling and not recoverable eval-side. The fix path is training-side.**

**Last commit:** `af3cb22` docs(h5): phase J stochastic-action eval evidence + tool.

**Current task:** Phase J evidence at `docs/h5-phase-j-stochastic-eval-evidence.md` (12 sections per GPT scope-note structure: TL;DR, why Phase I forces this slice, tool and methodology, model fingerprints carried forward, J0 results plus expansion-threshold check, J1 results, cross-model comparison, per-seed terminal-outcome diff table including seed 1008 highlight, verdict mapping to J-A through J-D, recommended next slice plus updated Grok status, reproduction recipe, implementation-not-at-fault statement). Tool at `tools/h5_stochastic_eval.py` is docs-and-tools-only; replicate seeding rule `policy_sample_seed = eval_seed + 10_000_000 + replicate_idx` with Python/NumPy/Torch reseeded before each episode. Per-tape summary JSONs and NDJSON step rows at `runs/phase_j/stochastic_eval_{j0,j1_g2}.{ndjson,summary.json}` plus Godot sidecars and driver logs (all gitignored). Tools reused: `h5_logit_compare.parse_models_arg` had to be replaced inline by `parse_models_arg_min1` because the original requires >=2 models and Phase J J0 runs only one.

**Next action:** GPT plans the training-side next slice per Phase J evidence section 10. Three candidate directions in increasing order of structural change: (1) training-time entropy probe on the existing entropy YAML with a logger that records when entropy collapse occurs, what the policy gradient norm and value-function/advantage signal pattern look like at and around collapse, and whether `ent_coef=0.01` is binding; (2) architecture change to `net_arch=dict(pi=[64], vf=[64])` giving the policy MLP a separate latent space; (3) train-seed asymmetry probe with a Grok consult on whether the seed=1 vs seed=2 raw-logit-margin asymmetry (Class A margins 6-11 vs Class B 0.8-6 at identical hyperparameters) is a known SB3 initialization pathology. Phase J recommends (1) first because it is cheapest and answers whether the existing setup can ever escape wedge basins given more training. Jeff approval required before execution.

**Blockers:** none.

**Notes:**

- Phase J seed-1008 falsification is the cleanest single-cell evidence that Class B's apparent survival is commitment-driven, not observation-driven. Deterministic E2 and G2 both timeout on seed 1008; stochastic E2 loses the timeout in all 5 replicates while stochastic G2 preserves it bit-identically. The difference is policy commitment to `left`: G2's 1.4% sampling deviation does not break the wedge, E2's 36% deviation does.
- Six of ten E2 seeds (1001, 1004, 1005, 1006, 1007, 1009) produce bit-identical episode lengths across all five replicates despite 36% non-argmax sampling. The env-terminal hazard on these seeds arrives at a specific frame that any non-`left` action at the wrong moment triggers, so policy commitment to `left` was buying exact step counts via hazard geometry, not learned dodging.
- Phase G shaping effect is now traceable through every layer: encoder norm std 8.5 → 10.5, `W[left]` row norm 0.025 → 0.055 (123% increase), raw margin 0.83 → 4.27 (5.1x), entropy 0.888 → 0.090 (10x lower), Phase J sampling effect 36% → 1.4%, Phase J trajectory effect 22% length drop → bit-identical. Each layer confirms commitment tightened.
- Operational. Python `-u` flag is mandatory when stdout redirects to a log file under cmd.exe `>> 2>&1`. Without `-u`, `print()` is block-buffered and per-episode progress lines do not appear until process exit. The J0 driver omitted `-u`; the J1 driver included it. NDJSON and summary JSON are unaffected by buffering. `h5_logit_compare.parse_models_arg` requires >=2 models; Phase J inlines `parse_models_arg_min1` to accept >=1.
- Phase J ruled out temperature, top-k, top-p sweeps as productive eval-side directions. The trained distribution's non-`left` mass is real but does not encode useful information; wider sampling produces worse outcomes, sharper sampling approaches G2's bit-identical-to-deterministic regime. The next slice is training-side per the evidence doc section 10.
